# 《构建你自己的物联网平台》翻译文档

## 第1章 那么……你想打造自己的物联网平台！
你热衷于打造自己的物联网平台，或者至少对了解打造过程感兴趣，这很棒。无论出于哪种原因，从一般意义上理解物联网平台的本质都很重要。首先，让我们看看物联网是什么。

在本章中，我将简要介绍物联网的背景以及本书中构建我们自己平台的相关内容。我们将讨论以下几个方面：
- 物联网平台的类型
- 优秀物联网平台的特点
- 为什么你应该打造自己的物联网平台

### 物联网的背景和我们的关注点
物联网（Internet of Things，简称IoT）是物理设备的网络，这些设备包括电器、智能手机、车辆、路灯、基础设施元件、工业机器等等，它们也被称为 “事物”（物联网中 “T” 的含义）。

凯文·阿什顿（Kevin Ashton）在宝洁公司工作时创造了 “物联网” 这个术语（尽管他更喜欢 “为事物的互联网” 这一表述）。起初，它只是一项借助RFID标签识别物理对象（即事物），并在软件系统中利用相关信息的实践。从那以后，情况发生了很大变化。许多变革和理念共同将物联网的范畴拓展得更为广泛。如今，物联网是一个融合了多种元素的体系，包括具有一定计算能力和内置智能的物理对象、使这些对象能够连接到互联网生态系统的媒介，以及整个互联网的计算机制，其范围涵盖从用户设备到计算机的各个层面。

从物联网平台的角度来看，我们关注的重点是物理对象首次接入互联网的环节，以及在软件和应用接管之前发生的关键过程。这些平台通常被称为中间件软件，因为它们处于物理对象和数字系统这两个不同事物的中间位置。中间件通常融合了高级和低级逻辑，并且结合了高级和低级语言来完成任务。

需要注意的是，我们并不打算构建一个功能完备、企业级的物联网平台。相反，我们将采用敏捷的方式，专注于在不投入大量资金的前提下缩短产品上市时间。我们的目标是设计出一个能够逐步扩展的平台，在扩展过程中无需进行大量返工，也不会对当前的工作状态造成潜在干扰。

虽然对于什么可被称为物联网平台并没有严格的定义，但人们普遍期望平台能够帮助高级软件、应用和系统与低级协议、通信方法以及各种异构对象进行交互。这种宽泛的定义或理解意味着符合这一标准的事物繁多。物联网平台是整个物联网生态系统的重要组成部分，但由于营销噱头和供应商的大量涌现，这个术语变得相当令人困惑。

### 市面上有多少种平台？
如今，市面上有超过500种不同类型和规模的平台，而且这个数字还在不断增长。有趣的是，许多平台正逐渐失去吸引力，它们要么停止运营，要么与其他平台合并。与此同时，一些平台正演变得更具前瞻性和强大功能。简而言之，这一领域的变化是双向的。

对这些平台进行概述后，我们可以将它们分为三大核心类型。
- **支持网络服务的平台**：这些平台支持网络服务部分，如MAC层通信解码器和转换器，主要控制和协调事物的电信部分。例如，LoRaWAN通信的网络服务器，这类平台将无线电级别的通信转换为原始数据信息，并将其传递给上游平台或应用进行进一步处理。除了网络服务平台，还有一些其他部分，如身份和密钥管理服务，以及它们的组合。
- **位于网络和应用之间的平台**：这些平台支持网络层之后、应用层之前的处理工作，例如系统级协议解码、转换、解密等。它们能够控制和协调协议以及整体通信流程，还支持驱动级逻辑和依赖它们的整个系统的底层架构。我们可以将它们视为系统的核心基础架构，这也是我们在本书中要构建的内容。
- **应用层开发平台**：这些平台支持在云端进行高级开发，大多帮助整合多个中间件平台、其他系统（如ERP和CRM）以及类似应用。这类平台与前两类（网络和中间件平台）的区别在于，如果高级平台出现故障，其他两个平台仍可继续运行，并可能支持高级平台中仍在正常工作的部分。相反，如果网络或中间件平台出现故障，整个系统和解决方案可能会出现停机。

鉴于我们有这么多类型的平台，市场上的选择又如此之多，定义一个优秀的物联网中间件平台应具备的要素就显得非常重要。

### 一个好的物联网平台应该具备什么？
对于每一款产品而言，其功能和特性都可分为必备和可选两类。当我们对两者加以区分时，答案就相对明晰了，打造自己的物联网平台更具意义。对于任何一个想要成为物联网一部分的中间件平台来说，必须具备以下功能和能力：
- **可扩展性**：与任何新的应用程序或产品一样，事物通常从小规模起步，而后逐渐发展壮大。因此，如果中间件平台要成为解决方案的核心，它必须能够按相同比例进行扩展。虽然扩展并非一蹴而就，但应确保在不破坏现有功能和不干扰现有生产设置的前提下，能够相对轻松地进行平台扩展。
- **可靠性**：一般来说，作为解决方案或产品核心的任何事物都应具备可靠性，这是显而易见的期望。中间件内置的冗余程度会根据最终应用、产品或行业领域而略有不同。例如，与咖啡机等家用电器的物联网平台相比，用于医疗设备、金融服务或安全系统的物联网平台，其所需的可靠性水平相对较高。
- **定制性**：由于我们正在构建自己的平台，它可以实现100% 的定制化。即便你打算购买现成的平台，也应确保在合理成本范围内进行定制。如果无法对中间件进行定制，那么你就不得不修改自己的产品或服务以适配该平台，这实际上是一种本末倒置的做法。
- **支持的协议和接口**：从基本定义来看，物联网中间件平台位于两个异构系统之间，即物理设备和云软件（设备类型和软件种类繁多）。该平台应能够与所有这些设备和软件进行协调，统一安排各项事务，并支持所有的语言或协议。此外，它还需具备在必要时创建所需插件以填补差距的能力，这样才能确保在进行全面升级之前的很长一段时间内，中间件平台都能保持良好的兼容性。
- **硬件无关性**：物联网本质上是由一组异构的连接事物、硬件设备、计算机系统和软件组成的。因此，对硬件的无关性要求几乎是不言而喻的。之所以仍需明确指出这一点，是因为存在一种稍有偏差的观点。许多人将硬件视为传感器的电子电路，基于这种观点，我们强调物联网平台应与你在电路中使用的任何电子元件无关。无论是开源硬件设计、专有电路，还是两者的混合，平台都应能够提供支持。
- **云无关性**：与硬件无关性类似，平台还需要具备云无关性。目前有众多云服务提供商，包括谷歌、微软和亚马逊网络服务（AWS）等，但平台不应依赖于特定的云服务。无论是你自己的服务，还是运行在网络附属存储（NAS）背后的第三方云服务，平台都应能正常工作。一个简单的合规性测试是看平台能否在裸机服务器上运行。也就是说，如果你获得一个虚拟专用服务器实例并安装该平台，它是否能够正常工作？答案应该是肯定的，这就表明该物联网平台具有云无关性。
- **架构和技术栈**：清晰明确的架构以及合适的技术栈组合是区分优秀物联网平台与其他平台的关键因素。平台可能构建于一些看似不太常见的技术组合之上，而这些技术可能在未来几年内面临淘汰，尤其是在你使用的运营期间。如果是这种情况，你应该避开这样的平台。同样的道理也适用于架构，即中间件的 “基础架构”。如果架构不够灵活，无法适应未来的变化，这就是一个危险信号。然而，完全灵活的架构也并非理想之选。你需要的是一个兼具灵活性和稳定性的架构，并由可靠、高效的技术栈提供支持。
- **安全性**：在过去几年里，物联网在很多应用和解决方案中，由于安全管理不善而饱受诟病。“物联网中的S代表安全” 这句话已变得十分常见，这强烈表明了中间件平台中的安全性与物联网生态系统的其他方面同样重要。如果你选择多租户平台，安全就成为一个至关重要的考虑因素。多租户特性使系统更容易受到攻击，因为即使你自己的应用运行良好，但同一平台上的其他应用（你的应用的共同租户）可能会给其他所有租户带来安全问题，这种风险始终存在。
- **成本**：为物联网平台设定的预算对成本因素有较大影响。总体而言，如果平台的成本（无论是内部构建还是购买现成的）与其功能和特性不匹配，就必须对其进行重新评估。简而言之，平台应具备足够的价值，以证明其成本的合理性。
- **支持**：持续的平台管理支持固然至关重要，解决方案集成方面的支持同样不可或缺。作为一项强制性要求，中间件平台应在解决方案的设计、开发、部署和管理的全过程中提供有力支持。

### 为什么你应该打造自己的物联网平台？
随着企业的发展和工作场景的演变，我们看到许多小型公司开始涉足物联网领域。然而，没有自己的物联网平台成为了这一发展进程中的障碍之一。

为什么不使用免费增值或免费试用的平台呢？当业务规模扩大时，成本会呈指数级飙升，这将带来更大的挑战。当试用期结束或免费增值服务无法满足需求时，用户会发现自己陷入困境，难以轻易切换到其他平台。这对许多小公司来说是一个巨大的挑战，拥有自己的物联网平台则是一个更好的解决方案。

一开始，购买现成的平台或选择免费增值服务可能看起来是更好的选择，但这其中存在权衡取舍。物联网平台可能会节省你的时间，但最终成本可能更高，这取决于供应商的定价方式。这主要是因为收费方式可能基于使用量或设备数量，此外，订阅费用会随着时间累积。虽然你能享受到前期成本显著降低的好处，即无需资本支出，但这也取决于你计划如何向购买物联网产品或解决方案的终端用户或客户收费。

最初成本较低的物联网平台可能会在时间成本上让你付出代价。这又回到了同一个问题：你花费的越少，就需要自己投入更多的时间和精力来集成和管理这些平台。如果你必须花费大量时间，那么将这些时间用于打造自己的平台，难道不是更好的选择吗？

打造自己的平台还支持在起步阶段节俭行事，然后仅在必要的时间和地点进行投资的理念。这将帮助你在不超出预算的情况下利用技术进步，更重要的是，你可以快速启动项目。本书将介绍如何在24小时内构建一个物联网平台，这与适应现成的或功能齐全的平台，以及学习使用免费试用或免费增值平台所需的较长时间形成鲜明对比。

如果你非常看重完全掌控权，那么一定要打造自己的解决方案。购买现成的解决方案通常意味着你需要调整自己的需求，对解决方案进行改造以适应现有的产品。这可能导致你构建出一个质量欠佳的解决方案，甚至可能是一个糟糕的方案。而打造自己的平台可以让你完全灵活地掌控自己的需求，包括如何构建以及何时构建。

从头开始打造自己的平台始终是一次充实的学习经历，如果可能的话，不应错过这样的机会。

### 总结
本章简要介绍了物联网的背景以及本书的关注领域。我们讨论了当前市面上的平台类型以及优秀物联网平台应具备的特点，并进一步阐述了打造自己的平台是一个不错选择的原因。接下来，让我们深入阅读。在下一章中，我们将了解物联网解决方案的组成部分，并进一步探讨解决方案生态系统。

## 第2章 物联网解决方案的组成部分
物联网的广泛热议出人意料地阻碍了人们对其工作原理的理解。如果你询问20个人物联网是如何工作的，可能会得到20种不同的答案。其中大多数答案可能涵盖的是物联网的成果或已实现的物联网解决方案的运作方式，而非物联网本身的工作原理。在物联网的背后，有许多技术支撑其得以实现。

在本章中，我将讨论以下内容：
- 物联网解决方案的关键组成部分
- 物联网平台的详细架构图
- 各组成部分如何协同工作
- 构建我们平台的建议方法

这些主题将帮助我们了解物联网的整体运作机制，进而有效地规划构建自己的物联网平台。

让我们首先讨论一些经常互换使用的术语。物联网解决方案和物联网应用之间存在差异。物联网解决方案通常指的是一个端到端的产品、服务，或两者的结合；而物联网应用通常是指IT软件、移动应用，或两者的组合。显然，物联网解决方案涵盖的内容比物联网应用要多得多。许多业务背景、客户背景和地缘政治背景都会对物联网解决方案产生影响。

然而，从物联网平台的角度来看，它处于物联网应用的边缘，通常是连接物理对象（即 “事物”）和软件系统的边界系统。图2 - 1展示了一个典型物联网解决方案的架构图，它以一种清晰的方式呈现了物联网解决方案的架构，将各个组成部分按照大型系统的重要方面进行了区分。
#### 物联网解决方案的功能模块

![image-20250414133819100](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414133819100.png)

从高层次来看，我们可以确定物联网解决方案由四个主要功能模块组成。如果缺少其中任何一个模块，都不宜称之为物联网解决方案。
- **设备（又称 “事物”）**：是物理传感器和执行器，它们测量各种参数并将其转换为电信号或数字数据。这些传感器要么连接到主机设备（常用于传统设备升级），要么集成到主机设备中（现代设备的常见做法）。这些设备是物联网应用的关键节点，通过作为输入、输出或兼具两者的功能，来实现完整的解决方案功能。典型的设备示例包括恒温器、智能捕鼠器、联网冰箱等。
- **网关**：是边缘设备，它与上游系统的通信方式有两种：有网关或无网关。有些设备具备通过各种通信协议（如REST、MQTT、AMQP、CoAP等）直接使用互联网协议（IP）进行通信的能力。这些能力通常得益于集成的通信模块，如Wi - Fi或GSM芯片，使设备能够连接到网络网关（如Wi - Fi路由器和移动基站）并直接与上游层进行通信。在这种情况下，路由器和移动基站就起到了网关的作用。然而，并非所有设备都具备直接连接互联网的能力，也并非都内置了必要的硬件。在这些情况下，它们需要借助其他设备将数据推送到上游层，而网关就能帮助设备实现这一点。通常，硬件网关采用双通信技术构建，使其能够通过一种通道与下游设备通信，通过另一种通道与上游层通信。典型的网关功能示例包括GSM和RF、GSM和蓝牙、Wi - Fi和蓝牙、Wi - Fi和XBee、LoRaWAN和以太网等组合。在某些情况下，智能手机也可作为网关，这在蓝牙低功耗（BLE）设备中更为常见。除了提供传输机制外，网关还可以提供一些可选功能，如数据隔离、清理、聚合、去重和边缘计算。
- **物联网平台**：是整个物联网解决方案的协调者，通常托管在云端。该模块负责与下游设备进行通信，并以极高的速度摄取大量数据。平台还负责以时间序列和结构化格式存储数据，以便进一步处理和分析。根据其内置的复杂程度，平台可能支持深度数据分析和其他操作。然而，物联网平台的核心功能是作为整个系统的协调者。
- **应用程序**：在大多数情况下，是整个解决方案面向最终用户的界面，必须以有意义的方式呈现给用户。这些应用程序可以是基于桌面的、基于移动设备的，或者两者皆有。应用程序还以各种方式丰富来自平台的数据，并以可用的格式呈现给用户。此外，这些应用程序在接口层面与其他系统和应用程序进行集成，实现应用程序间的数据交换。一个典型的操作示例是配备跟踪移动应用程序的库存跟踪设备，将数据提供给用户，并将数据输入到ERP系统进行库存管理。
#### 物联网平台的详细架构图
我们对第三个模块——物联网平台的机制更感兴趣。让我们来看看一个物联网平台要有效运行应具备的所有基本要素。

![image-20250414133850203](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414133850203.png)

图2 - 2展示了一个典型物联网平台的架构图。

互连的箭头表示每个模块之间的数据和信息流。每个模块都代表了平台的主要功能组件。该平台安装在虚拟云机器或虚拟专用服务器（VPS）上。强烈建议使用基于Linux的操作系统，如Ubuntu、Centos、Debian、OpenWRT或LEDE，以获得更好的性能、安全特性和对平台的整体控制。无论使用这些操作系统中的哪一种，其概念和模块级架构都不会改变。

![image-20250414133950713](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414133950713.png)

- **边缘接口、消息代理和消息总线**：该模块负责与物理世界进行交互，尤其是与异构设备和传感器进行通信。由于设备可能通过多种通信技术（如Wi - Fi、蓝牙、LoRaWAN、GPRS等）进行通信，因此该模块需要支持所有这些技术。我们可以采用模块化的方式来实现这一点，即分别处理每种类型的通信协议。例如，支持Wi - Fi的设备可以通过REST API进行通信，适用于受限设备；也可以采用基于MQTT的消息代理，以发布/订阅的方式实现通信。对于基于LoRaWAN（长距离广域网）的设备，主消息代理有一个额外的插件，用于与LoRaWAN网络服务器进行通信并对数据包进行解码。

  注意，发布/订阅（pub - sub）指的是通信的发布 - 订阅范式，将在第6章中详细解释。

  该模块有效地将整个平台与设备解耦，支持现代物联网设备的多种边缘接口和协议。无论使用何种通信介质、网络类型和协议，消息代理的任务都是以统一的方式整合数据，并将其推送到公共消息总线。所有其他功能模块都通过这个消息总线进行进一步的操作。消息代理起到消息协调者和整合者的作用。

  ![image-20250414134102996](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134102996.png)

- **消息路由器和通信管理**：一旦消息出现在主消息总线上，可能需要添加更多上下文信息或进行优化，以便其他模块使用。有些消息需要进行功能增强和添加额外信息，这取决于设备部署的上下文和应用程序的要求。通信管理模块负责对现有数据消息进行功能增强、重新广播到消息总线、在主消息到达后发布额外的上下文信息和其他消息，并进行适当的标记。通信管理功能与消息代理和规则引擎模块进行协调，并根据需要与设备管理器进行交互。

  此外，通信管理模块还执行格式转换任务，例如将数据从CSV转换为JSON，或从二进制转换为文本格式等。我们还可以让它执行一些操作，如消息去重。去重是指从设备中消除或丢弃多个重复消息或冗余数据包的过程，因为它们可能没有任何用处。去重方案取决于设备或传感器的类型，我们需要根据具体情况进行实施，尽管方法是相同的。作为通信路由器，该模块可以控制平台上的进一步消息传递和通信。

  ![image-20250414134131098](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134131098.png)

- **时间序列存储和数据管理**：顾名思义，该模块按顺序（即时间序列方式）存储消息总线上接收到的所有已解析数据。虽然数据存储不是物联网平台的核心功能，平台外部的模块也可以处理存储，但从协调和编排的角度来看，这是一项必不可少的活动。通常，通信和路由模块，甚至消息代理本身，出于特定功能目的需要近期数据，此时这种存储就派上了用场。对于许多物联网应用，用户更倾向于将数据从物联网平台中提取出来，存储在应用数据仓库中进行进一步处理。因此，它通常用于临时存储设备数据，并不适合存储大型数据集。

  ![image-20250414134230471](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134230471.png)

- **规则引擎**：在我看来，这是一个非常强大的模块，它为平台提供了增强的功能。规则引擎是一个执行模块，它监控消息总线和平台上的事件，并根据设定的规则采取行动。例如，一个典型的规则引擎功能可能是：“当下游设备发送包含‘ka - boom’关键字的数据包时，触发并广播警报消息”。规则引擎持续监听消息总线的广播。当通信模块将来自下游设备的解码数据包发送到消息总线上时，规则就会触发。然后，规则引擎向消息总线广播另一条消息（警报）。由于这一切都在物联网平台内部以及紧密协作的模块之间发生，因此执行速度非常快。规则引擎还有助于构建用于解码和丰富从设备接收的现有数据的模块化规则，从而增强通信模块的功能。此外，很容易实现对其他模块、应用程序、程序和系统的回调。

  ![image-20250414134251237](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134251237.png)

- **REST API接口**：RESTful API对于那些不需要持续或实时连接和访问的支持功能和实用程序非常有用。虽然通常由上游程序和应用程序使用，但下游设备在需要时也可以访问这些API。一个典型的用例是具有Wi - Fi连接的温度传感器，它每15分钟发送一次读数。由于两次读数之间的时间间隔较长，因此不需要实时连接或始终保持连接状态。简单的HTTP操作就可以相对更高效地完成数据发送任务。在这种情况下，传感器可以通过REST API将数据发送到平台。REST API与消息代理和通信管理器协同工作，将接收到的数据发布到主消息总线上；它还可以使用时间序列数据库记录向传感器发送响应。这个响应可能包含一些额外信息，以便传感器在下次工作时能够以特定方式运行。该API模块还支持数据聚合和批量操作功能，例如上游应用程序查询多条记录。通过这种方式，上游应用程序和系统与核心平台模块保持解耦，从而维持功能的划分并确保安全性。可以构建各种基于角色的认证机制来访问API。REST API模块还可以与规则引擎交互，允许应用程序在任何给定时间配置或触发特定规则。这也使得下游设备能够使用相同的功能，当设备需要自动启动某些工作流程而不是由应用程序触发时，这一功能会非常方便。例如智能锁，当前门有活动需要房主注意，而房主不在家时，上游应用程序可能会在智能锁报告活动时通知用户，然后等待用户响应或采取进一步行动。如果用户无法响应，应用程序可以触发预定义操作的规则。如果警报的严重程度较高，设备可以配置为不等待用户操作或响应，而是直接触发默认工作流程（例如通知安保人员等）。这些功能在设计和运营自主智能设备群时非常实用。

  ![image-20250414134328744](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134328744.png)

- **微服务**：除了数据管理、操作和交换功能外，物联网平台还需要一些支持功能才能有效运行。诸如短信或电子邮件通知、验证、验证码、社交媒体认证或支付服务集成等服务，都是这些辅助服务的示例。这些服务被整合在微服务模块中。如果平台内经常使用某些功能，可以将其打包并分离到这个模块中，使其与主流平台区分开来。分离和打包后，这些功能可以在平台内部和外部的模块中进行复用。

  ![image-20250414134345956](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134345956.png)

- **设备管理器**：当平台开始托管大约50个或更多设备时，管理工作可能会变得困难。此时，有必要建立某种中央控制机制来管理设备。这就是设备管理器模块发挥作用的地方。它主要提供将设备作为资产进行管理的通用功能，包括列出所有设备、它们的活动/非活动状态、电池电量、网络状况、访问密钥、读数、存储数据访问、设备详细信息、会话信息等类似内容。设备管理器还帮助管理一批设备的无线更新，或为系统管理员提供中央监控功能。在某些用例中，设备需要访问权限，并且用户可能被分配对一组设备的特定访问权限。有了设备管理器，管理这种访问权限矩阵就变得容易了。

  ![image-20250414134405042](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134405042.png)

- **应用和用户管理**：该模块提供与设备管理器类似的功能，不同之处在于它为上游应用程序和用户提供服务。典型的用户管理功能，如密码和凭据、访问密钥、登录和权限等，都通过这个模块进行管理。对于上游应用程序和各种其他集成系统，API密钥、凭据和访问权限也可以通过同一模块进行管理。虽然这看起来更像是应用程序级别的功能，但将其作为物联网平台的一项功能进行绑定，有助于将其与整体架构紧密集成。物联网是一个系统的集合，异构系统是其特点之一。我们最不希望看到的就是这些系统功能不同步。
#### 这个架构中的所有模块都是必需的吗？
不是的。虽然这八个模块定义了一个架构良好的物联网平台，但并非所有模块都是必需的。特定的用例或行业领域可能会对此有不同的定义。在平台开发的初始阶段，你可能不需要所有模块，它们可以在平台开发生命周期的后期添加。

核心功能模块，即设备接口和消息代理、消息路由器和通信模块、数据存储、设备管理和规则引擎，对于物联网平台的有效运行至关重要。其他模块，如REST API、微服务以及应用和用户管理，虽然很实用，通常能让工作更轻松，但并非必需，也不会妨碍物联网平台的功能。

在从头开始开发我们的物联网平台时，我们会暂时搁置这些功能，只有在时间和资源允许的情况下才会实现它们。
#### 建议的方法
为了在最短的时间内开发出物联网平台，我们不仅要以模块化的方式进行开发，还要采用敏捷的方法。每个模块都将规划好功能和特性，进行开发，然后部署到云端进行测试。一旦我们测试了单个模块并确认其按预期工作，就可以进入下一个模块的开发。

第一步，我们将为平台搭建云环境。接下来，设置开发第一个模块（边缘接口和消息代理）所需的基本组件。逻辑上的下一步是设置时间序列数据存储。然后，我们将为平台开发基本的REST API，接着开发消息路由器功能。

在搭建好平台的基本框架后，我们将开发一些微服务。之后，我们会多次迭代这些模块，为平台打造一个稳定的核心。

一旦我们对核心功能感到满意，就可以设置规则引擎，然后是设备管理功能。由于应用和用户管理属于非必要模块，我们会在最后对其进行评估。
#### 总结
在本章中，我们讨论了物联网平台的功能模块，并确定了构建自己平台的方法。在下一章中，我们将讨论构建平台的基本要求，涵盖所需元素的详细规格，以及获取这些元素的方式和地点。第3章还将结合我们的计划工作，进一步阐述平台功能架构图。 

## 第3章 构建自己平台的必备要素
在开始构建平台的核心工作之前，打下坚实的基础非常重要。这不仅能让我们的平台在长期内具备可扩展性和稳定性，还有助于我们清晰地阐明当前和未来的技术需求。在本章中，我们将：
- 为云实例选择操作系统
- 列出实例的基本规格
- 确定本地计算机访问所需的条件
- 详细阐述我们自己的物联网平台架构图
#### 确定云实例的具体规格
要构建自己的平台，我们至少需要一个裸金属云实例。虽然在这类实例中有多种操作系统可供选择，但最推荐的是基于Linux的操作系统。让我们来看看它为何是首选。
- **总体拥有成本**：最明显的优势是Linux是免费的，而Windows不是。单个用户许可证的成本可能不高，但随着时间推移，总体拥有成本会更高，从而增加持续运营成本。
- **可靠性**：基于Linux的系统比Windows更可靠。传统上，Linux系统以能够连续运行数年而无需重启著称。这是为我们的物联网平台选择操作系统的关键标准。
- **硬件资源**：与Windows相比，Linux系统消耗的系统资源（如RAM、磁盘空间等）更少。对于我们的物联网平台，我们至少需要1GB的RAM和20 - 25GB的磁盘空间。也就是说，如果选择基于Linux的系统，成本能够得到控制。而Windows系统在这样的配置下可能无法高效运行。
- **安全性**：基于Linux的系统在设计之初就考虑了安全性，是更安全环境的首选。由于其固有的安全性，我们可以节省防病毒软件的成本和额外的系统开销。
- **控制权**：控制权是构建自己的物联网平台的主要原因之一。基于Linux的系统在各个层面都提供了控制权。没有臃肿的软件对像我们平台这样追求速度的系统来说非常重要。能够控制安装的内容有助于我们牢牢掌握这种控制权。
- **功耗**：目前，Windows只能在不符合低功耗要求的设备上运行，而我们的系统通常需要经常或始终保持运行状态，对低功耗有一定需求。
#### 其他规格
为了保证基本功能的有效运行，我们在操作系统上至少需要1GB的RAM和20 - 25GB的磁盘空间。我们还需要Node.js和Node - RED软件来编写平台代码。

对于基于Linux的系统，我们需要在系统上安装LAMP栈。LAMP栈是一组用于创建网站和Web应用程序的开源软件，是Linux - Apache - MySQL - PHP的首字母缩写，它由Linux操作系统、Apache HTTP服务器、MySQL关系数据库管理系统和PHP编程语言组成。

除了这些基本软件包，我们还需要一些附加组件，随着进程的推进，我们会列出这些组件。一旦我们的实例安装并运行了LAMP栈、Node.js和Node - RED，就具备了继续进行的基本基础设施。
#### 从哪里获取云实例？
在虚拟专用服务器上创建云实例有不少选择，如AWS（亚马逊网络服务）、谷歌云、阿里云和DigitalOcean等，而且可能很快还会出现更多选择。

选择哪个供应商的云实例取决于你的供应商偏好、这些供应商提供的实例定价以及许多其他因素。在很多方面，DigitalOcean似乎是个不错的选择，主要是因为它提供了简洁明了的用户界面，没有过多不必要的选项和选择干扰。这是保持敏捷并快速完成任务的关键。

从经济实惠的角度来看，DigitalOcean显然是最佳选择，因为与其他供应商复杂的毫秒级计算定价相比，它的定价透明。其价格基于按小时计费，并且通常在每月使用的情况下是固定的。

DigitalOcean不像AWS或谷歌云那样是托管服务，但这对我们的目的来说应该没问题。DigitalOcean服务器上的云实例需要由所有者自行管理，包括升级和打补丁等，而AWS和谷歌云则不是这样。然而，在处理裸金属云实例时，情况并非那么简单，所以即使使用谷歌云和AWS，如果是裸金属实例，你也必须自行管理系统。

简而言之，如果你有大规模的实施需求，应该选择AWS或谷歌云；对于其他所有目的，DigitalOcean是更好的选择。

对于我们的平台，我们追求敏捷性（即在24小时内构建完成）、成本效益、计费透明度、完全控制权以及其他类似方面。所以在本书中，我们将使用DigitalOcean作为我们的云实例。不过，如果你对其他供应商更熟悉，也完全没问题。
#### 我们自己的机器呢？
虽然我们大部分工作会在云实例中完成，并且大部分开发也会在云环境中进行，但我们仍然需要在本地机器（笔记本电脑或台式机）上安装一些工具。

一开始，我们至少需要三种工具/软件。
- **用于SSH的终端模拟器**：我们将使用一个名为PuTTY的程序，它是一款知名且广泛使用的终端模拟器。它是一个免费的开源程序，支持多种网络协议，包括SSH、SCP、Telnet等，还允许进行原始套接字连接，并且可以连接到计算机的串行端口，这在测试我们平台上的一些硬件模块时可能会派上用场。
- **基本编辑器**：这个编辑器可以像Notepad程序一样基础，我推荐Notepad++。它是一款用于Microsoft Windows的免费软件文本编辑器，支持在单个窗口中处理多个打开的文件，非常方便。该项目的名称来源于C语言中的增量运算符。
- **FTP程序**：FTP应用程序有多种选择，包括WinSCP、FileZilla、CoreFTP和FireFTP等。在本书中，我们将一直使用FileZilla。

PuTTY、Notepad++和FileZilla对我们的目的来说很有效且适用，但它们不是必需的。你可以自由选择其他可用的选项。
#### 详细阐述物联网平台架构图
在第2章中，我们讨论了物联网平台的详细架构图。现在我们将确定哪些模块需要优先进行快速开发，以及在第一阶段需要开发哪些功能。
- **边缘接口、消息代理和消息总线**：这将是我们物联网平台的基本功能，我们会在一开始就着手开发。我们将使用MQTT协议进行消息交换，因为MQTT几乎是边缘设备和物联网应用通信的事实上的标准协议，我们稍后会详细讨论MQTT。这将是我们物联网平台最关键的模块之一。
- **消息路由器和通信管理**：一开始，我们将只开发一个基本的消息路由器，它不会具备主要的通信管理功能。我们将把这个模块作为开发第二阶段的占位符。
- **时间序列存储和数据管理**：如前一章所述，这不是核心功能，但它是我们在第一阶段要构建以便后续使用的元素之一。
- **REST API接口**：为了测试从设备到平台以及从Web应用程序到平台的功能，REST API是必不可少的。我们将从基本的API开始，然后在后续阶段添加复杂的功能。
- **微服务**：虽然我们要到第二阶段才会开发微服务，但我们会提前做一些准备工作，以确保在后期添加微服务时，无需对基本设计进行重大更改。从设计上讲，微服务在物联网平台中并非关键部分。
- **规则引擎**：由于这是物联网平台最强大的功能之一，我们从一开始就要重视它。规则引擎无法一蹴而就，我们需要多次迭代才能使其完善。
- **设备管理器和应用管理器**：虽然这些功能很实用，但它们不是平台的核心部分，所以我们不会为我们的物联网平台开发这些功能。实际上，在没有正式的设备管理器和应用管理器的情况下，也可以轻松使用大量设备和应用。
#### 我们自己的物联网平台架构图
既然我们已经列出了开发的重点领域，修订后的物联网平台架构图大致如图3 - 1所示。

![image-20250414134551815](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134551815.png)

简而言之，从核心物联网平台的角度来看，我们的平台将具备完整的功能。所有被视为功能特性的部分都将留待未来进行增强。这是在速度和功能之间做出的良好权衡，绝不会损害该平台的产品性能。
#### 总结
到目前为止，我们对云实例的规格做出了各种选择，列出了在笔记本电脑上构建平台所需的内容。

下一步，我们将为物联网平台的REST API创建一个需求清单，并详细说明我们希望每个API实现的功能。我们还将列出边缘接口、消息代理、数据库管理器、消息路由器和一些微服务的需求。 

## 第4章 创建我们的平台需求清单
尽管边做边规划可能会更轻松，但提前尽可能多地定义需求，将有助于我们更快、更高效地构建所需的平台框架。

在本章中，我们将列出物联网平台中每个模块的预期和一般需求。我们将讨论以下内容：
- 我们（以及设备）如何与平台进行实时连接
- 我们希望如何存储数据
- 我们将构建的API类型
- 我们需要构建的微服务和实用工具
#### 与平台实时连接
Web应用程序面临的挑战之一是实时通信能力。虽然同步通信很常见，并且我们可以通过典型的类似HTTP的请求来实现，但使用相同的格式和技术无法有效地实现异步通信。然而，与物联网平台进行实时连接和通信是物联网解决方案和应用的关键要求。这就是我们需要使用消息代理并实现类似发布 - 订阅机制的原因。

这也是消息代理成为最新Web技术重要组件的关键原因。消息代理通常是中间件程序，借助发布 - 订阅机制为所有连接的应用程序和设备提供异步通信能力。

发布 - 订阅机制是一种有趣的范式，它不要求通信双方同时在线。此外，它还使得任何一方都可以在不考虑对方是否准备好的情况下发起数据传输。这与HTTP的工作方式完全相反，在HTTP中，客户端必须发起请求，服务器才会响应，服务器无法实时联系客户端。当我们通过消息代理使用发布 - 订阅机制连接服务器和客户端时，双方都可以发送数据，这是一项强大的功能。

简而言之，我们需要一个消息代理程序。选择的消息代理必须满足某些基本标准，一般来说，两个重要标准是易于配置和维护，以及在生产环境中足够稳定。
#### 使用MQTT作为消息代理
虽然消息代理技术有很多种，但我们将使用MQTT标准，因为它几乎是物联网应用和解决方案的事实标准协议。

MQTT代表MQ遥测传输，是一种发布 - 订阅、极其简单且轻量级的消息传递协议，专为受限设备以及低带宽、高延迟或不可靠的网络而设计。其设计原则是在确保可靠性和交付保证的同时，尽量减少网络带宽和设备资源需求。这些原则使该协议非常适合新兴的机器对机器（M2M）或物联网连接设备领域，以及对带宽和电池电量要求较高的移动应用（mqtt.org，2013）。

MQTT有许多实现方式，包括商业版和开源版。Mosquitto是一个流行的开源MQTT实现，我们将使用它来构建我们的消息代理。我们也可以使用任何其他基于Node.js的MQTT开源实现来构建消息代理，以后可以探索这个选项，它可用作我们平台冗余的备用二级代理。
#### 我们希望如何存储数据？
到目前为止，我们已决定使用Mosquitto作为MQTT消息代理。然而，消息代理不是存储提供商，它们更像是消息传递的载体，消息或数据通过它们传递。这些数据是临时的，如果不存储，以后就无法查看或检索。

从平台的角度来看，我们需要这种存储和检索机制，以便能够在以后检索数据；对于非同步的应用程序和设备，这些数据可以作为信息的影子副本。

由于我们在安装了LAMP栈的Ubuntu服务器上构建平台，MySQL是默认且明显的选择。不仅如此，根据2018年DB - Engines排名，MySQL一直是第二受欢迎的数据库。

关键问题是我们希望如何存储数据。这里所说的数据是指通过消息代理和中央消息总线通信管理器的事务数据。这些数据只有几个用于数据处理和审计目的的信息字段，因此，我们的数据存储模式必须适合这些数据。

在MQTT通信中，每个消息数据包都包含两个字段：主题（topic）和有效负载（payload）。主题通常用作数据的键，而有效负载是实际的数据或内容。由于MQTT是一种消息传递协议，并不一定指定有效负载的格式，所以我们有一定的灵活性。然而，为了在整个平台上保持可扩展性和统一的方法，我们将在整个平台上对有效负载（即数据包）使用JSON（JavaScript对象表示法）编码。这不仅有助于保持一致性，还将使我们的平台具有可扩展性，并且易于适应新的变化。
#### 数据存储模式
JSON数据本质上是一个ASCII字符串，也是MQTT消息中的主题。需要注意的是，MQTT也支持二进制数据包，其中可能包含非ASCII字符。这意味着我们可以轻松通过消息代理传输二进制文件和数据，在设计平台时应牢记这一点。

除了存储主题和相关的数据有效负载外，我们还需要为存储的每条消息分配一个唯一ID。最重要的是，由于这将是一个时间序列数据库，我们需要存储每条消息的时间戳。在这个阶段，除了这些字段，我们在物联网平台的核心中不需要存储任何其他信息。基于这些考虑，我们的数据库表结构如图4 - 1所示。
| 序号 | 名称      | 类型          | 是否为空 | 额外信息 |
| ---- | --------- | ------------- | -------- | -------- |
| 1    | ID        | int(11)       | 否       | 自动递增 |
| 2    | Topic     | varchar(1024) | 否       |          |
| 3    | Payload   | varchar(2048) | 否       |          |
| 4    | Timestamp | varchar(15)   | 否       |          |

以下是对每列的简要解释：
- **ID**：自增唯一编号，我们使用MySQL的自动递增功能来实现。
- **Topic**：声明为varchar类型，以便在该字段中存储可变长度的数据。主题的长度可以任意，并且根据应用程序的不同而变化。我们将限制其长度为1KB，这对于任何可能的主题名称来说都足够大。
- **Payload**：数据包的大小较大且长度可变（因此使用可变类型）。不过，目前我们将数据包存储限制为2KB。请记住，这些是MySQL的可配置选项，可以在不影响应用程序的情况下进行更改。我们可以增加大小限制而不影响先前存储的数据，但降低大小时，先前的数据可能会被截断，这可以根据需要决定。
- **Timestamp**：我们将存储UNIX（即基于纪元的时间戳），它是UNIX风格的日期时间戳，以整数格式表示。纪元（或UNIX时间、POSIX时间、UNIX时间戳）是自1970年1月1日（午夜UTC/GMT）以来经过的秒数，这里不考虑闰秒。这可能不是一个精确的时间戳，但对于现实场景来说已经足够接近，满足我们的应用目的。

基于这个表结构，我们将把接收到的每个数据包存储在Payload列中，并将其主题存储在Topic列中，两者均按原样存储。时间戳将取自我们平台的系统时间，ID将自动递增。这将使我们能够在需要时按存储顺序并参考时间戳查询数据，使其成为一个时间序列数据集。
#### 通过API访问平台资源
有了Mosquitto MQTT代理和时间序列存储后，我们的平台一般就能接收数据包并通过MQTT进行通信。这种（通过MQTT的）通信将基于数据流，如果没有平台的其他部分，不一定具备任何内置智能。

连接到数据流的设备或应用程序能够实时访问数据，但在离线或未连接时，没有机制来请求数据。这就是我们的API将发挥重要作用的地方。

在计算机编程领域，API指应用程序编程接口，它是一组子程序或子过程定义、通信协议和用于构建软件的工具。一般来说，它是一组明确定义的计算机程序或系统各组件之间的通信方法。一个好的API通过提供所有构建模块，使程序员能够为实现特定目的将它们组合在一起，从而更轻松地开发计算机程序。

让我们将API分为四种不同类型，这将有助于我们保持开发的模块化和可插拔性。
- **数据访问API**：这些API帮助我们访问物联网平台中的时间序列数据存储，并进行有限的操作。此外，该API有助于在实时数据流（基于MQTT）和非实时数据流（基于HTTP）之间建立链接。
- **实用工具API**：许多应用程序在非定期基础上可能需要某些实用工具。这些实用工具的一个典型示例是特定格式的数据转换。如果应用程序或设备需要一次性对数据进行编码或加密，或者需要在特定条件下对其进行转换，那么它可以使用其中一些API。本质上，它们是平台内外多个资源共享的封装函数。
- **微服务API**：基于功能或服务于特定预定义目的的端点属于这一组。这些通常是应用服务，如电子邮件和短信服务。
- **插件API**：我们构建的一些接口将连接平台中原本未连接的两个部分。其中一些API还充当移动或计算机应用程序的前端。
#### 数据访问API
为了安全、适当地访问时间序列数据，我们将设计一组API以满足各种场景和需求。一般来说，我们至少需要七个端点。注意，每个需求都进行了编号，以便我们在本书中轻松引用。数据需求以D开头，而微服务和实用工具需求以M开头。
- **D1. 获取单个数据记录**：使应用程序和设备能够根据指定的主题或主题模式从时间序列数据存储中查询单个数据记录。
- **D2. 按序列获取多个数据记录**：使应用程序和设备能够根据指定的主题或主题模式查询多个数据记录。
- **D3. 根据特定条件获取一个或多个记录**：使应用程序能够根据指定条件（针对主题、有效负载或两者）查询一个或多个数据记录。条件可以是主题或有效负载模式，或者与时间戳相关，例如某个时间段内的数据。
- **D4. 通过API存储数据记录（如果未通过MQTT流发送）**：除了从时间序列存储中查询数据外，我们还希望应用程序和设备能够将数据存储在时间序列存储中。这对于无法通过实时MQTT数据流进行通信的设备和应用程序很有用。
- **D5. 删除单个数据记录**：使应用程序或设备能够根据指定主题删除单个数据记录。请注意，我们不希望实现主题模式机制，以避免意外的数据清除。
- **D6. 按序列删除多个数据记录**：从数据集中删除基于主题的一组数据记录。如果我们希望保持数据存储的精简，这很有用。此需求的一个典型场景是删除24小时后的所有数据，或者结合多记录查询，将数据从平台存储中取出并存储在其他地方用于审计或监管目的。
- **D7. 根据特定条件删除一个或多个记录**：与根据指定条件查询一个或多个数据记录类似，我们可能需要从时间序列存储中删除它们。虽然这是一个有用的功能，但它需要内置一定的安全级别，我们将详细讨论这一点。
#### 基本微服务和实用工具
这里我们列出一些希望在物联网平台上经常（但不频繁）使用的微服务和实用工具。
- **M1. 发布当前时间戳**：我强烈推荐在分布式应用程序中使用此服务。我们经常发现，由于时区差异和系统时钟限制，系统之间无法协调。我们可以借助时间广播服务来克服这个问题。另一种替代方法是使用NTP（网络时间协议），但并非所有应用程序或设备都能访问NTP服务器，这限制了它们进行时间同步操作的能力。我们将使用此实用工具从我们自己的物联网平台发布/广播时间值，以便所有系统都与我们的平台同步。我们可以单独将平台与NTP服务器同步，无论如何，我们的平台都提供了一个稳定的参考源。
- **M2. 获取当前时间戳**：这是发布当前时间戳功能的轮询服务。当设备或应用程序错过先前的时间戳广播且无法等到下一次广播，或者用户或业务规则强制设备或应用程序进行同步时，此服务很有帮助。
- **M3. 获取唯一或随机数/字符串**：这是一个非常方便的服务，用于生成和使用随机字符串和数字。我们可以使用随机生成的数字和字符串创建唯一键或参考编号，也可以将它们用作随机密码或令牌。
- **M4. 获取UUID**：UUID（通用唯一识别码）类似于随机数或字符串生成服务，但更具结构性且在全球范围内唯一。UUID算法保证在公元3400年（即从现在起1500年后）之前生成的任何UUID都极有可能与其他UUID不同。与随机字符串类似，我们可以使用UUID为设备和应用程序生成密钥或密码。
- **M5. 发送电子邮件**：这是许多应用程序和平台可能经常使用的普遍服务。我们需要电子邮件服务用于自动化、警报、用户检查和验证、密码重置、关键通信等。这是我们物联网平台必备的服务。
- **M6. 发送短信**：我们可以将短信用于与电子邮件类似的目的。此外，我们还可以在需要额外安全层的系统或关键部分中使用它来实现双因素身份验证。我们的应用程序和连接到平台的其他应用程序都可以使用此服务。
- **M7. MQTT回调注册**：由于MQTT数据馈送是实时的，对于仅依赖HTTP机制的应用程序而言，除非应用程序持续或频繁轮询，否则无法收到新数据可用的通知。为避免这种情况，我们开发了一种服务，本质上是创建一个Webhook，每当平台接收到与给定主题或有效负载标准匹配的数据包时使用。这样，仅使用HTTP的应用程序可以通过REST API（如D4中所述）发送或传输数据包，并通过此服务接收（得到通知）。我们可能需要利用规则引擎来编写此服务。请注意，此服务仅适用于基于服务器的应用程序，硬件设备不太可能从中受益。
#### 路由和过滤数据与消息
数据流量和消息的路由与过滤在第一阶段只是一个通用架构，并非最终版本。我们将根据新设备和应用程序的添加不断对其进行改进。
#### 我们的物联网平台更新架构图
请记住，我们列出的所有需求都不是绝对的。其中许多需求可以在以后构建，或者完全跳过。到目前为止，我们已经定义了平台四个主要模块的基本需求。

我们构建平台所采用的敏捷方式使我们能够在这些模块中的任何一个中添加更多功能和特性。通过这种方式，我们可以在不到24小时的时间内使核心功能的物联网平台启动并运行，然后持续对其进行扩展。我们的物联网平台更新架构图如图4 - 2所示。

![image-20250414134725284](C:\Users\moboe\AppData\Roaming\Typora\typora-user-images\image-20250414134725284.png)

#### 总结
在本章中，我们做出了一些与数据存储模式和实时连接相关的关键决策。我们还定义了API、微服务和实用工具的需求。现在我们开始行动并着手构建。

下一章完全是实践操作。因此，你可能需要确保笔记本电脑上安装了所有软件实用工具。我们还需要为平台准备一个完全合格的域名。考虑并选择一个域名是个不错的主意。 

## 第5章 动手搭建！
有了需求清单，并且对物联网平台的模块架构有了详细了解后，就该开始动手搭建了。

在本章中，我们将：
- 初始化云实例
- 根据需要安装基础和高级软件栈
- 为基础软件栈添加安全配置
- 创建并配置时间序列数据库
- 为我们的平台设置域名

这是本书中最长且非常重要的一章。为了充分利用这部分内容，我建议你先通读整章，然后再进行实际操作。这样，如果有任何遗漏或需要的内容，你就不会在操作过程中遇到问题。
#### 初始化云实例
这是搭建平台的第一步。建议你准备好上一章讨论的所有先决条件。
- **注册并创建**：首先，我们在DigitalOcean上注册并初始化一个实例，然后安装基础软件栈。前往www.digitalocean.com进行注册。注意，如果你是DigitalOcean的新用户，可以访问以下链接获得10美元的推荐奖金：https://bit.ly/in24hrs。注册并登录到控制面板后，我们就可以开始创建新的云实例。DigitalOcean（DO）将这些实例称为 “水滴”（droplets）。为了简单起见，在整个过程中我们将云实例称为 “实例”。如果这是你第一次登录且没有已存在的实例，你可能会看到一个大的蓝色按钮来创建实例。或者，你也可以点击页面右上角的绿色 “创建” 按钮，从下拉菜单中选择 “水滴” 选项。更多详细信息请参考图5 - 1。这将带你进入实例创建页面。在这个页面上，你需要为我们的实例指定各种配置选项，如选择内存、存储空间等。你还可以选择扩展或其他附加功能。虽然大多数流行的默认选项已经预先选择，但我强烈建议你对每个规格进行谨慎选择。
- **选择操作系统镜像**：在这一步，我们要为物联网平台选择操作系统。如前所述，我们将选择Ubuntu发行版，不过还有许多其他选项可供选择。更多详细信息请参考图5 - 2。在 “容器发行版” 下，我们可以看到容器化的操作系统镜像。在 “一键应用” 部分，有许多预配置的选项可供快速启动。这个选项看起来很有吸引力，但花时间选择我们要安装的内容是值得的。对于高级用法，DigitalOcean提供了自定义镜像选项，我们可以上传自定义的操作系统镜像。在许多情况下，当我们想在多个实例中复制物联网平台时，这个选项非常方便。这样，我们可以简单地复制已安装好所有内容的现有镜像，并将其上传到新实例中，这使得操作更加快速且可扩展。
- **选择实例规格**：选好操作系统镜像后，我们需要指定所需的RAM和磁盘空间大小。由于我们需要大约2GB的RAM和50GB的磁盘空间来保证基本功能的有效运行，所以我们将从左侧标准表格中选择第二个选项，如图5 - 3所示。未来，如果我们需要扩展实例规格，只需点击几下，大约5分钟即可完成。请记住，我们之前讨论过构建基础架构的方式，要使其能够按需扩展，这就是实现的方法。选择2GB RAM和50GB磁盘空间后，我们将获得2TB的数据传输额度。这对于刚开始来说已经足够，并且在平台变得非常繁忙之前，这些资源在很长一段时间内都能满足需求。需要注意的是，CPU优化实例更适合那些严重依赖CPU而非RAM或磁盘和IO的应用程序。我们的物联网平台最终会发展成为企业级系统，但在这个阶段，我们只选择标准选项，这也有助于控制预算。当我们选择64位操作系统时（如之前步骤所做的那样），建议使用4GB或更多的RAM，因为在有限内存下运行64位系统会失去其优势。从经验来看，目前的选择不会带来太大问题，而且升级很容易，所以我们按目前的选择继续进行。
- **选择数据中心区域**：接下来是为云实例选择数据中心区域，如图5 - 4所示。唯一影响这个选择的标准是我们的用户所在位置。将服务器设置在离最终用户地理位置较近的地方可以通过减少服务器延迟来提高性能。如果用户群体预计分布在全球，那么选择位于地球中心区域的数据中心是有意义的，因为这样可以使每个人的延迟几乎相同。虽然这不是最佳选择，但对于刚开始使用单个小云实例的情况来说，这是最好的选择。这就是我们为物联网平台选择伦敦数据中心的原因，因为它更靠近协调世界时（UTC）。我们还将选择一些额外选项，以便轻松使用专用网络和监控云实例。专用网络选项为实例在同一数据中心内提供了一个额外的网络接口。这样，如果我们在同一数据中心有多个云实例，就可以直接相互通信，而无需将流量路由到数据中心之外。IPv6选项则允许通过IPv6访问我们的云实例。请记住，IPv6主要是为了面向未来，所以如果暂时不需要，也可以合理地忽略（不勾选）这个选项。选择 “监控” 选项会在操作系统中添加DigitalOcean代理，用于收集扩展指标并创建警报策略。在本书撰写时，这个功能是免费的，有助于监控和了解与我们云实例相关的流量和其他指标。
- **完成并创建实例**：选择完所有选项后，就该完成创建过程并为云实例选择一个主机名了，如图5 - 5所示。主机名用于控制面板信息和服务器的主机名。输入主机名，将 “水滴”（云实例）数量保持为1，因为我们只创建一个实例，然后点击 “创建” 按钮。这将显示云实例创建的进度条。创建过程完成后，会为我们的云实例分配一个IP地址，并显示在控制面板上，如图5 - 6所示。请记录下这个IP地址，因为在整个过程中都会用到它。在图5 - 6中，分配给我们云实例的IP地址是139.59.164.101。请注意，你所分配的IP地址会有所不同。在本书中，你应该用自己的IP地址替换我的IP地址。在某些地方，使用了符号表示，如<INSTANCE_IP>，请记得用你实际的IP地址替换它。
#### 连接到我们的云实例
云实例创建完成后，DigitalOcean会发送一封电子邮件，其中包含首次登录所需的IP地址、用户名和临时密码。请在收件箱中查看这封邮件，它会发送到你注册账户时使用的电子邮件地址。

如果你使用的是Linux或macOS，可以直接在终端中使用ssh命令连接到云实例。连接命令如下：
```bash
# ssh root@<INSTANCE_IP>
```
我已经验证过，在最新的Windows命令行中，上述命令也可以正常使用。根据你的IP地址，命令会有所变化。请注意，我们这里的用户名是root。通常，默认的Ubuntu操作系统发行版用户名是root。由于我使用的是Windows机器，所以我使用PuTTY软件进行连接。PuTTY程序的主对话框如图5 - 7所示。

首次连接时，PuTTY会显示一个安全警报对话框，提示服务器的主机密钥未缓存在注册表中。只有在确认连接安全的情况下，我们才应该连接到这台计算机。因为我们连接的是自己的云实例，所以点击 “是” 按钮进入下一步。

一开始，系统会提示更改密码。我们首先输入通过电子邮件收到的默认（临时）密码，然后可以将其更改为自己选择的新密码。

登录后，命令提示符会改变，并且我们会看到一个欢迎屏幕（见图5 - 8），这标志着初始化云实例的第一步完成。

虽然不是必需的，但我们也可以为云实例创建一个浮动IP（也称为弹性IP）。它本质上提供了一个额外的公共静态IP地址，我们可以使用它来访问云实例，而无需替换其原始IP。浮动IP在创建高可用性云平台时非常有用。你可以在www.digitalocean.com/docs/networking/floating - ips/上了解更多关于浮动IP的信息。
#### 安装基础软件栈
现在我们的云实例已经初始化并准备好进行进一步部署，接下来的合理步骤是安装LAMP栈。LAMP栈是一组用于创建网站和Web应用程序的开源软件，是Linux - Apache - MySQL - PHP的缩写，它由Linux操作系统、Apache HTTP服务器、MySQL关系数据库管理系统和PHP编程语言组成。

在开始任何安装之前，让我们先启动基本安全设置。我们将从在实例上启用防火墙开始。Ubuntu服务器使用防火墙，通过启用它，我们可以确保只允许与某些服务的连接。我们可以使用这个应用程序轻松设置基本防火墙。

简单防火墙（Uncomplicated Firewall，UFW）是一个用于管理netfilter防火墙的程序，设计得易于使用。它使用由少量简单命令组成的命令行界面，并使用iptables进行配置。UFW在所有8.04 LTS之后的Ubuntu安装中默认可用（简单防火墙，无日期）。

应用程序安装后可以向UFW注册其配置文件。UFW通过名称管理这些应用程序，我们可以看到，我们用来连接云实例的SSH实用程序已经在OpenSSH下注册。我们可以使用以下命令查看已注册的应用程序：
```bash
# ufw app list
```
输出：
```
Available applications:
OpenSSH
```
为了确保启用防火墙后允许SSH连接，我们将执行以下两个命令来允许这些连接并启用防火墙：
```bash
# ufw allow OpenSSH
# ufw enable
```
当我们执行启用防火墙的命令时，系统会提示这可能会中断操作，此时按Y表示确认。防火墙启用后，我们可以使用status命令检查其状态：
```bash
# ufw status
```
输出：
```
Status: active
```
此时，除了SSH，防火墙会阻止所有连接。每次安装新应用程序时，我们都必须配置它以接受传入流量。
- **安装Apache**：Apache HTTP服务器，通常简称为Apache，是一款免费的开源跨平台Web服务器软件，根据Apache许可证2.0版发布。Apache由Apache软件基金会支持的一个开放社区的开发者进行开发和维护（Apache，无日期）。它是世界上最受欢迎的Web服务器之一，有详细的文档且应用广泛，因此是一个不错的选择。我们可以使用Ubuntu的软件包管理器apt来安装Apache，安装只需要两个命令。在安装过程中，它会提示所需的额外磁盘空间，持续按Y并按回车键继续，直到安装完成。
```bash
# apt update
# apt install apache2
```
注意，我们选择了单核实例来满足我们的目的。然而，当你升级到多核处理器时，Apache可能无法提供最佳性能，最终应该评估像NGINX这样的选项。

现在我们已经启用了防火墙，尽管安装了Apache，但Web服务器流量仍然不被允许。我们必须将Apache配置文件添加到防火墙配置中。使用以下命令允许Web流量：
```bash
# ufw app list
```
输出：
```
Available applications:
Apache
Apache Full
Apache Secure
OpenSSH
```
```bash
# sudo ufw app info "Apache Full"
```
输出：
```
Profile: Apache Full
Title: Web Server (HTTP,HTTPS)
Description: Apache v2 is the next generation of the omnipresent Apache web server.
Ports: 80,443/tcp
```
```bash
# sudo ufw allow in "Apache Full"
```
输出：
```
Rule added
Rule added (v6)
```
在上述命令中，第一个命令显示了所有在UFW下有应用程序配置文件的应用程序。由于我们添加了Apache，所以它会显示在命令输出中。第二个命令检查 “Apache Full” 配置是否允许在HTTP（端口80）和HTTPS（端口443）端口上进行Web流量。第三个命令将该配置文件添加到UFW程序中。

此时，如果我们打开Web浏览器并导航到云实例的IP地址，应该会看到默认的Apache网页（见图5 - 9），这表明Apache已安装并正常运行。
- **安装MySQL**：安装好Web服务器并使其运行后，就该安装数据库管理系统了。由于我们选择了MySQL，安装过程很简单，只需几个命令，如下所示：
```bash
# apt install mysql-server
```
执行上述命令时，系统会提示所需的额外磁盘空间，我们持续按Y并按回车键继续，直到安装完成。显然，MySQL安装时带有默认配置，现在对其进行安全设置是个好主意。MySQL安装后有一个预安装脚本，可帮助我们保护数据库系统。要开始这个过程，执行以下命令：
```bash
# mysql_secure_installation
```
这会询问我们是否要配置密码验证插件。我们选择Y并继续按提示提供额外的密码。当我们设置新密码时，脚本会显示我们输入的root密码的强度，如果需要，我们有机会更改它。此时，我们可以跳过此步骤并输入N。

对于其余的问题，我们在脚本的每个提示处持续按Y和回车键。这将基本上删除一些默认的匿名用户和测试数据库，还会禁用远程root登录并加载这些新规则，以便MySQL立即执行我们刚刚所做的更改。

对于运行最新MySQL版本的Ubuntu系统，root用户默认使用auth_socket插件进行身份验证，而不是密码。在许多情况下，这会使访问变得复杂，特别是当我们希望其他应用程序和外部程序连接到MySQL时。我们需要更改它，可以使用以下命令，首先在命令行中打开MySQL提示符：
```bash
# mysql
```
输出：
```
Welcome to the MySQL monitor. Commands end with ; or \g.
Your MySQL connection id is 7
Server version: 5.7.24 - 0ubuntu0.18.10.1 (Ubuntu)
Copyright (c) 2000, 2018, Oracle and/or its affiliates. All rights reserved.
Oracle is a registered trademark of Oracle Corporation and/or its affiliates. Other names may be trademarks of their respective owners.
Type 'help;' or '\h' for help. Type '\c' to clear the current input statement.
```
```bash
mysql> SELECT user,authentication_string,plugin,host FROM mysql.user WHERE user="root";
```
输出：
```
+------| user | authentication_string | plugin | host |
| root | | auth_socket | localhost |
+------+-----------------------+-------------+-----------+
1 row in set (0.00 sec)
```
第二个命令列出了root用户的身份验证方法。正如我们所看到的，该方法不是我们想要的（即不是密码验证），所以我们使用另一个命令进行更改。在执行第一个命令时，我们需要准备一个强密码。
```bash
mysql> ALTER USER 'root'@'localhost' IDENTIFIED WITH mysql_native_password BY 'your - password';
```
输出：
```
Query OK, 0 rows affected (0.00 sec)
```
```bash
mysql> FLUSH PRIVILEGES;
```
输出：
```
Query OK, 0 rows affected (0.00 sec)
```
```bash
mysql> SELECT user,authentication_string,plugin,host FROM mysql.user WHERE user="root";
```
输出：
```
+------| user | authentication_string | plugin | host |
| root | *A0AF1999141933B3B4C7AE72544AB01849669F98 | mysql_native_password| localhost |
+------+-----------------------+-------------+-----------+
1 row in set (0.00 sec)
```
```bash
mysql> exit
```
输出：
```
Bye
```
第一个命令设置你选择的强密码。第二个命令重新加载表，使新更改立即生效。下一个命令再次检查root用户的身份验证方法状态，从输出中可以看到它已按需要更改。然后，我们可以使用exit命令退出MySQL提示符。

此时，MySQL已安装，我们可以安装LAMP栈的最后一个关键组件——PHP。
- ### 安装PHP
  PHP即超文本预处理器（Hypertext Preprocessor），是一种用于开发网络应用程序和服务的开源服务器端脚本语言。

  我们将使用Ubuntu的apt软件包管理器来安装PHP。
  ```bash
  # apt install php libapache2-mod-php php-mysql
  ```
  如你所见，除了PHP软件包，我们还安装了一些其他软件包。这将使我们能够在Apache网络服务器上运行PHP代码，并轻松与MySQL数据库进行通信。执行上述命令时，系统会提示所需的额外磁盘空间。我们持续按Y然后按Enter键继续，直到安装完成。

  默认情况下，Apache网络服务器优先提供HTML文件，如果找不到HTML文件，则会查找CGI和Perl文件。如果也找不到CGI或Perl文件，它会检查是否有PHP文件。然而，由于我们希望在所有情况下都使用PHP作为服务器端程序，因此需要更改此行为。我们使用以下命令更改Apache目录配置：
  ```bash
  # nano /etc/apache2/mods-enabled/dir.conf
  ```
  这会在默认的Ubuntu编辑器nano中打开配置文件。该文件按顺序列出了默认的文件名，如下所示：
  ```xml
  <IfModule mod_dir.c>
      DirectoryIndex index.html index.cgi index.pl index.php index.xhtml index.htm
  </IfModule>
  ```
  首先，我们更改文件名的顺序，将index.php放在首位，然后是index.html，最后是其他文件。
  ```xml
  <IfModule mod_dir.c>
      DirectoryIndex index.php index.html index.htm index.cgi index.pl index.xhtml
  </IfModule>
  ```
  更改完成后，按Ctrl+X，然后输入Y，再按Enter键保存文件并退出编辑器。

  我们使用以下命令重启Apache网络服务器，使这些更改生效：
  ```bash
  # systemctl restart apache2
  ```
  这会静默重启Apache网络服务器并重新加载新的配置。不过，我们仍然需要验证这些更改是否有效。为此，我们创建一个测试PHP程序文件，并通过在浏览器中导航到云实例的IP地址来进行验证。要创建一个新的测试程序，我们使用以下命令打开一个新文件，并在文件中添加几行基本内容：
  ```bash
  # nano /var/www/html/test.php
  ```
  添加以下内容：
  ```php
  <?php
  echo("Hi...PHP is working !");
  ?>
  ```
  完成后，按Ctrl+X组合键，然后输入Y，再按Enter键保存并关闭文件。现在，当我们导航到http://<INSTANCE_IP>/test.php时，应该会看到如图5-10所示的消息。
  ![测试PHP页面](https://i.imgur.com/xxxxxx.png)
  *图5-10 在浏览器中显示正常工作的PHP页面*

  此时，我们的LAMP栈已完全正常运行，但在进一步使用之前，我们需要加强安全性。我们还希望能够在网络浏览器环境中方便高效地访问MySQL，借助phpMyAdmin程序可以实现这一点。

  ### 保护实例和软件安全
  由于我们需要一种简单高效的方式从浏览器访问MySQL功能，同时保持安全访问，phpMyAdmin似乎是一个更好的选择。phpMyAdmin是一个用于MySQL的免费开源管理工具。作为一个主要用PHP编写的便携式网络应用程序，它已成为最受欢迎的MySQL管理工具之一（phpMyAdmin，无日期）。

  要开始安装，我们首先更新云实例的软件包索引。然后使用以下两个命令安装phpMyAdmin的基础文件：
  ```bash
  # apt update
  # apt install phpmyadmin php-mbstring php-gettext
  ```
  需要注意的是，虽然php - mbstring（多字节字符串操作）和php - gettext（文本处理）不是与安全相关的软件包，但它们是phpMyAdmin正常运行所必需的，因此需要安装。

  在安装过程的这个阶段，系统会询问一些问题。在第一个屏幕上，我们将选择apache2作为服务器；使用空格键选择，使用箭头键移动选项。选择后，安装继续，然后会出现另一个提示，询问是否使用dbconfig - common为phpmyadmin配置数据库。选择“是”。

  最后，系统会要求你选择并确认用于phpMyAdmin的MySQL应用程序密码。输入密码后，安装完成。此时，安装程序已将phpMyAdmin的Apache配置文件添加到/etc/apache2/conf - enabled/目录中，该文件会自动被读取。我们现在唯一需要做的是明确启用mbstring PHP扩展，我们可以通过输入以下命令来实现：
  ```bash
  # phpenmod mbstring
  # systemctl restart apache2
  ```
  现在，我们可以通过访问http://<INSTANCE_IP>/phpmyadmin来使用phpMyAdmin访问MySQL数据库。它会要求输入凭证，即我们刚刚创建的密码。输入正确的凭证后，我们就能在浏览器中访问MySQL数据库，如图5-11所示。
  ![phpMyAdmin登录界面和主界面](https://i.imgur.com/xxxxxx.png)
  *图5-11 phpMyAdmin登录界面和浏览器中的主界面*

  让我们来保护phpMyAdmin实例的安全。当我们在浏览器中导航到phpMyAdmin页面时，会注意到该应用程序要求输入凭证。然而，如果页面不是通过SSL访问的，我们的凭证很可能会被拦截。此外，phpMyAdmin工具使用非常广泛，常常成为攻击目标。不过，我们可以通过在应用程序前面设置另一个凭证网关来增加一层安全保护。这意味着用户在输入一级凭证之前无法导航到登录页面。

  我们可以借助Apache网络服务器中常用的方法（即使用.htaccess）、身份验证和授权功能来添加这一额外的安全层。

  第一步，我们通过编辑phpMyAdmin的Apache配置文件来启用.htaccess文件覆盖功能。该文件位于配置目录中。
  ```bash
  # nano /etc/apache2/conf-available/phpmyadmin.conf
  ```
  将文件内容修改如下：
  ```xml
  Alias /phpmyadmin /usr/share/phpmyadmin
  <Directory /usr/share/phpmyadmin>
      Options SymLinksIfOwnerMatch
      DirectoryIndex index.php
      AllowOverride All
     .....
     .....
  </Directory>
  ```
  请注意，在上述文件片段中，我们在<Directory /usr/share/phpmyadmin>部分添加了一个指令AllowOverride All，这启用了.htaccess文件用于身份验证和授权的功能。现在我们将创建这个所需的.htaccess文件，其中包含四行用于启用身份验证的内容。
  ```bash
  # nano /usr/share/phpmyadmin/.htaccess
  ```
  向文件中添加以下内容：
  ```
  AuthType Basic
  AuthName "Restricted Files"
  AuthUserFile /etc/phpmyadmin/.htpasswd
  Require valid-user
  ```
  通过添加上述内容，我们启用了一种基本类型的身份验证，它借助密码文件来完成。文件中的第二行设置了身份验证对话框的消息。作为一般准则和最佳实践，我们保持消息通用，简单表述为“Restricted Files”，这样消息就不会透露过多关于限制背后内容的信息。

  第三行指定了密码文件的名称及其位置。理想情况下，它应该位于受保护目录之外。最后一行声明了授权功能，阻止未经授权的用户进入该目录并访问phpMyAdmin应用程序。我们按Ctrl+X组合键保存并关闭文件，然后输入Y，再按Enter键。

  下一步是创建这个密码文件并使用htpasswd实用工具添加第一个用户及其密码，使用以下命令：
  ```bash
  # htpasswd -c /etc/phpmyadmin/.htpasswd username
  # htpasswd /etc/phpmyadmin/.htpasswd additional_username
  ```
  第一个命令创建名为.htpasswd的新文件，并向其中添加一个新用户。执行此命令后，系统会提示输入密码并进行确认，密码会以哈希格式存储在文件中。

  请注意，第二个命令没有使用-c选项，因此它不会创建新文件，而是使用第一个命令创建的现有文件，并根据需要添加另一个用户。我们可以根据需要添加任意数量的用户。

  现在，如果我们导航到http://<INSTANCE_IP>/phpmyadmin，浏览器会在显示phpMyAdmin登录页面之前首先要求进行Apache身份验证，如图5-12所示。输入正确的凭证后，我们会看到通常的phpMyAdmin登录页面。
  ![phpMyAdmin的额外安全验证](https://i.imgur.com/xxxxxx.png)
  *图5-12 phpMyAdmin的额外安全验证*

  此时，phpMyAdmin已完全配置好并可投入使用。使用这个界面，我们可以轻松创建数据库和表，并执行各种数据库操作。这在我们继续创建时间序列数据存储并向平台中添加其他数据表时非常有用。

  ### 有域名更方便
  虽然目前通过IP地址访问我们的云实例很方便，但使用合适的完全限定域名（FQDN）会更加便捷，并且这还能让我们添加基于TLS/SSL的传输层安全性。

  显然，Let's Encrypt制定了一项政策，即不为IP地址颁发SSL证书。如果我们继续使用IP地址，将无法使用该证书。从技术上讲，我们可以安装证书，但它毫无用处。此外，许多浏览器不认可基于裸IP地址的SSL。所以，现在为我们的物联网平台获取一个域名是个好主意。获取所需域名有多种选择。在谷歌上进行简单搜索就能找到最适合你的选项。

  第一步是从域名注册商处获取所需的域名，然后前往DigitalOcean控制面板对云实例的DNS记录进行必要的更改。在本书中，我使用自己的域名in24hrs.xyz进行后续的相关操作，而不是使用通用的example.com域名。

  获得所需的域名后，在控制面板上点击“添加域名”，分别如图5-13和图5-14所示。
  ![在云实例上添加新域名](https://i.imgur.com/xxxxxx.png)
  *图5-13 在云实例上添加新域名*
  ![输入域名并点击添加](https://i.imgur.com/xxxxxx.png)
  *图5-14 输入域名并点击添加域名*

  接下来，我们在域名注册商处更新域名服务器。这个过程因注册商而异，每个域名注册商都有分步教程来解释如何在其控制面板上更改域名服务器。请注意，DigitalOcean有以下三个域名服务器，需要在域名注册处进行更新：
  ```
  ns1.digitalocean.com
  ns2.digitalocean.com
  ns3.digitalocean.com
  ```
  在配置好域名并在控制面板上更新了相关记录后，我们为域名设置SSL证书。SSL证书有不同的提供商，其成本从几百美元到数千美元不等。不过，有许多可靠的机构会免费提供SSL证书，且不影响安全性；我们将使用其中一个。如果你已经从其他机构购买了SSL证书，可以将其上传到云实例，然后直接进入Apache配置更新步骤。

  请注意，在执行这些步骤和命令时，请使用你选择的域名。

  在开始下一步之前，你需要在服务器控制面板上设置以下两条DNS记录，如图5-15所示。
  ![在控制面板中更改A记录](https://i.imgur.com/xxxxxx.png)
  *图5-15 在控制面板中更改A记录*
   - 一条A记录，将in24hrs.xyz指向你的服务器公共IP地址。
   - 一条A记录，将www.in24hrs.xyz指向你的服务器公共IP地址。

  通常，域名服务器和记录的更改需要大约一个小时才能在网络上完全生效。一个小时后，如果我们在浏览器中导航到www.in24hrs.xyz，它应该会带我们进入云实例上的默认Apache网页，如图5-9所示。

  ### 为Web服务器添加虚拟主机
  我们需要为Apache设置虚拟主机，以便有效地使用和配置我们的域名。第一步是为新添加的域名创建一个单独的目录。然后使用$USER环境变量分配该目录的所有权。
  ```bash
  # mkdir -p /var/www/in24hrs.xyz/html
  # chown -R $USER:$USER /var/www/in24hrs.xyz/html
  ```
  让我们确保权限设置正确。
  ```bash
  # chmod -R 755 /var/www/in24hrs.xyz
  ```
  现在我们将创建一个简单的网页，当在浏览器中导航到我们的域名时会显示该网页。由于我们在前面的设置中已经优先使用PHP，所以我们将创建一个PHP文件。
  ```bash
  # nano /var/www/in24hrs.xyz/html/index.php
  ```
  向文件中添加以下内容：
  ```php
  <?php
  echo("Hi...this is our webpage with domain name !");
  ?>
  ```
  为了让Apache网络服务器提供这个内容，我们需要创建一个包含正确指令的虚拟主机文件，然后启用该配置。我们还将关闭网络服务器的默认配置，并保留一份单独的副本，以供将来参考和作为备用选项。
  ```bash
  # nano /etc/apache2/sites-available/in24hrs.xyz.conf
  ```
  向文件中添加以下内容：
  ```xml
  <VirtualHost *:80>
      ServerAdmin admin@in24hrs.xyz
      ServerName in24hrs.xyz
      ServerAlias www.in24hrs.xyz
      DocumentRoot /var/www/in24hrs.xyz/html
      ErrorLog ${APACHE_LOG_DIR}/error.log
      CustomLog ${APACHE_LOG_DIR}/access.log combined
  </VirtualHost>
  ```
  ```bash
  # a2ensite in24hrs.xyz.conf
  ```
  输出：
  ```
  Enabling site in24hrs.xyz.
  ```
  ```
  To activate the new configuration, you need to run:
  systemctl reload apache2
  ```
  现在我们将禁用默认配置，然后运行配置测试，检查过程中是否有错误。如果你收到错误信息，请检查虚拟主机文件中是否有拼写错误和遗漏的字符。
  ```bash
  # a2dissite 000-default.conf
  ```
  输出：
  ```
  Site 000-default disabled.
  ```
  ```
  To activate the new configuration, you need to run:
  systemctl reload apache2
  ```
  ```bash
  # apache2ctl configtest
  ```
  输出：
  ```
  Syntax OK
  ```
  ```bash
  # systemctl restart apache2
  ```
  执行最后一条命令后，Apache网络服务器会重启并重新加载我们之前创建的新配置。此时，如果我们导航到www.in24hrs.xyz，应该会在网络浏览器中看到“Hi...this is our webpage with domain name!”的消息，如图5-16所示。
  ![通过域名访问云实例](https://i.imgur.com/xxxxxx.png)
  *图5-16 现在我们能够通过域名访问云实例*

  ### 安装SSL证书
  在这个过程中，我们将使用Let's Encrypt，它是一个证书颁发机构（CA），提供了一种简单且自动化的方式来获取、安装和维护免费的TLS/SSL证书。借助一个名为Certbot的软件客户端，这个过程得以简化和自动化。Certbot会尝试自动完成几乎所有必要的步骤，只需要少量的手动操作。

  我们将从活跃的Ubuntu软件仓库安装Certbot，这样通常能获取到最新版本，使用以下命令：
  ```bash
  # add-apt-repository ppa:certbot/certbot
  ```
  按Enter键接受提示，安装将继续进行。然后我们使用apt软件包管理器安装Certbot的Apache软件包。
  ```bash
  # apt install python-certbot-apache
  ```
  由于我们已经为防火墙启用了UFW，并且已经通过Apache Full配置启用了HTTPS流量，所以此时防火墙设置应该已经就绪。如果尚未完成此操作，则需要在继续之前进行设置。

  现在我们使用以下命令为我们的域名获取SSL证书：
  ```bash
  # certbot --apache -d in24hrs.xyz -d www.in24hrs.xyz
  ```
  这个命令会使用Apache插件运行Certbot。-d参数指定我们请求SSL证书的域名。由于这是我们第一次运行此命令，它可能会提示输入电子邮件地址。接受服务条款等内容后，脚本将以详细输出的方式继续运行，如下所示：
  ```
  Performing the following challenges:
  http-01 challenge for in24hrs.xyz
  http-01 challenge for www.in24hrs.xyz
  Enabled Apache rewrite module
  Waiting for verification...
  Cleaning up challenges
  Created an SSL vhost at /etc/apache2/sites-available/in24hrs.xyz-le-ssl.conf
  Enabled Apache socache_shmcb module
  Enabled Apache ssl module
  Deploying Certificate to VirtualHost /etc/apache2/sites-available/in24hrs.xyz-le-ssl.conf
  Enabling available site: /etc/apache2/sites-available/in24hrs.xyz-le-ssl.conf
  Deploying Certificate to VirtualHost /etc/apache2/sites-available/in24hrs.xyz-le-ssl.conf
  ```
  一旦证书部署到我们的云实例上，它会询问我们是否要强制进行HTTPS更改；我们选择选项2，配置将相应更新。配置更新后，Apache网络服务器会重新加载配置并重启。
  ```
  Please choose whether or not to redirect HTTP traffic to HTTPS, removing HTTP access.
  1: No redirect - Make no further changes to the webserver configuration.
  2: Redirect - Make all requests redirect to secure HTTPS access. Choose this for new sites, or if you're confident your site works on HTTPS. You can undo this change by editing your web server's configuration.
  Select the appropriate number [1-2] then [enter] (press 'c' to cancel): 2
  ```
  ```
  Enabled Apache rewrite module
  Redirecting vhost in /etc/apache2/sites-enabled/in24hrs.xyz.conf to ssl vhost in /etc/apache2/sites-available/in24hrs.xyz-le-ssl.conf
  ```
  ```
  Congratulations! You have successfully enabled
  https://in24hrs.xyz and
  https://www.in24hrs.xyz
  ```
  上述设置还确保HTTP流量会重定向到HTTPS。要检查一切是否按预期工作，在浏览器中导航到www.in24hrs.xyz，你应该会看到如图5-17所示的输出。我们的证书会自动下载、安装和加载，并且由于Certbot的自动化功能，它们将每季度自动更新。
  ![启用HTTPS的域名](https://i.imgur.com/xxxxxx.png)
  *图5-17 域名上启用了HTTPS*

  当导航到www.in24hrs.xyz时，你会注意到浏览器现在会在网站名称旁边显示一个锁形图标（通常在Firefox中是绿色的，在Chrome中是灰色的）。

  我们可以在www.ssllabs.com/ssltest/analyze.html?d=www.in24hrs.xyz上检查我们云实例的SSL证书，该网站会进行一些测试，并通过SSL报告告知我们被测试网站的安全级别，如图5-18所示。
  ![SSL证书有效且评级为A](https://i.imgur.com/xxxxxx.png)
  *图5-18 SSL证书有效且评级为A*

  此时，我们的通用Web界面已准备就绪，可以通过SSL使用。我们还能够通过SSL使用phpMyAdmin，这将进一步增强数据库的安全性。

  ### 安装Node.js和Node - RED
  Node.js是一个用于通用编程的JavaScript平台，允许用户快速构建网络应用程序。通过在前端和后端都使用JavaScript，Node.js使开发更加一致和集成（DigitalOcean，无日期）。

  我们将使用Ubuntu的apt软件包管理器在云实例上更新并安装Node.js，使用以下命令：
  ```bash
  # apt update
  # apt install nodejs
  # apt install npm
  ```
  在上述三个命令中，第一个命令刷新本地软件包索引，第二个命令在实例上安装Node.js。我们还安装了节点包管理器NPM，它可以帮助我们根据需要更新和添加节点包到实例中。

  对于每个安装过程，系统都会提示磁盘空间的使用情况；我们在两种情况下都选择Y表示确认。

  安装成功后，我们可以使用以下命令检查每个应用程序的安装版本：
  ```bash
  # nodejs -v
  ```
  输出：
  ```
  v8.11.4
  ```
  ```bash
  # npm -v
  ```
  输出：
  ```
  5.8.0
  ```
  安装完节点包管理器后，使用相同的包管理器安装Node - RED会更加容易。我们将把Node - RED作为全局模块安装，这样它会将node - red命令添加到云系统的路径中。
  ```bash
  npm install -g --unsafe-perm node-red
  ```
  上述命令会一次性安装Node - RED及其依赖项。理想情况下，如果我们通过浏览器导航，应该能够直接使用Node - RED。然而，回想一下，我们使用UFW启用了防火墙，并关闭了所有未经明确批准的入站流量。因此，在未启用相关流量的情况下，我们无法访问Node - RED。

  此外，由于我们还强制要求所有入站流量都通过HTTPS，所以需要修改Node - RED的配置以启用HTTPS。默认情况下，Node - RED可以在没有SSL的情况下运行。Node - RED运行在默认端口1880上；因此，我们首先要在该端口上启用流量。
  ```bash
  # ufw allow 1880/tcp
  ```
  输出：
  ```
  Rule added
  Rule added (v6)
  ```
  此时，我们可以运行Node - RED，并通过使用公共IP地址访问我们的网站（http://<INSTANCE_IP>:1880）来检查默认界面是否加载，如图5-19所示。这是因为我们尚未为Node - RED启用SSL，所以无法直接使用域名访问它。现在使用以下命令运行Node - RED，然后导航到上述地址。
  ```bash
  # node-red
  ```
  ![使用公共IP地址访问Node - RED管理面板](https://i.imgur.com/xxxxxx.png)
  *图5-19 使用公共IP地址访问Node - RED管理面板*

  ### 修改Node - RED设置
  现在让我们修改Node - RED的配置，以实现更好的可访问性和功能性。这主要涉及修改位于node - red根目录下的settings.js文件。我们将使用系统上任何标准的编辑器（如nano）来进行这些更改。

  请注意，在这个阶段，我们可以使用任何兼容的FTP程序（如FileZilla），使用正常的root登录凭据访问云实例。FileZilla使我们能够从云服务器下载文件，然后打开、编辑并保存文件，最后再将其上传回实例。

  这可以通过三个简单的步骤完成。
  1. 使用公共IP地址和登录凭据将FileZilla FTP客户端连接到我们的云实例。
  2. 找到你想要编辑的文件。然后右键单击该文件，这将使用你计算机上安装的默认编辑器程序打开该文件。在大多数情况下，它是Notepad、TextMate或Notepad++。
  3. 根据需要编辑文件，然后保存。当你保存文件时，FileZilla会显示一个窗口，提醒你文件已被更改，并询问你是否要将文件上传回服务器。如果文件已存在，它还会询问你是否要覆盖现有文件。点击“是”选项后，新编辑/创建的文件将被上传到目标文件夹中。

  现在让我们回到编辑settings文件。从文件开头开始，我们取消对fs模块声明的注释，使其可用，如下所示：
  ```javascript
  // The `https` setting requires the `fs` module. Uncomment the following
  // to make it available:
  var fs = require("fs");
  ```
  启用fs模块是因为我们希望在Node - RED实例上启用HTTPS。在settings文件的第93行，我们取消对admin root path的注释。这使我们能够在不同于根URL的端点访问Node - RED编辑器。虽然这不是必需的，但它很有用，因为这样我们就可以将根URL用于其他目的，比如托管不同的网页和信息页面等。

  ```javascript
  // 以下属性可用于指定不同的根路径。
  // 如果设置为false，则此功能将被禁用。
  httpAdminRoot: '/admin',
  ```
  现在让我们修改138行到147行之间的代码块。这个代码块用于为Node - RED启用HTTPS，我们必须提供私钥和证书文件名来启用它。我们必须提供完整的文件路径，获取路径有两种方式。我们可以参考SSL安装输出，在安装过程脚本的末尾会显示文件的存储位置；或者也可以从位于/etc/apache2/sites - available/in24hrs.xyz.conf的Apache配置文件中复制路径。
  ```javascript
  // 以下属性可用于启用HTTPS
  // 有关其内容的详细信息，请参阅http://nodejs.org/api/https.html#https_https_createserver_options_requestlistener
  // 有关如何加载此设置所使用的`fs`模块的说明，请参阅此文件顶部的注释。
  https: {
      key: fs.readFileSync("/etc/letsencrypt/live/in24hrs.xyz/privkey.pem"),
      cert: fs.readFileSync("/etc/letsencrypt/live/in24hrs.xyz/cert.pem")
  },
  ```
  因为我们正在构建自己的物联网平台，该平台最终将在各种系统和设备上使用，所以需要跨域资源共享（CORS）。现在在settings文件中启用它是个不错的主意，取消162行到165行之间相关代码块的注释，如下所示：
  ```javascript
  // 以下属性可用于配置HTTP节点中的跨域资源共享。
  // 有关其内容的详细信息，请参阅https://github.com/troygoode/node-cors#configuration-httpNodeCors: { }, options。以下是一组基本的宽松选项：
  origin: "*",
  methods: "GET,PUT,POST,DELETE"
  ```
  一个完全修改后的settings.js文件可以从GitHub存储库下载。

  ### 保护我们的Node-RED编辑器
  新文件保存到磁盘后，现在我们可以使用域名通过HTTPS而不是公共IP来运行Node-RED实例。通过导航到我们的网站`www.in24hrs.xyz:1880/admin`来检查编辑器界面是否加载。

  你可以直接看到编辑器，而没有任何身份验证或登录页面，现在我们将通过为Node-RED启用安全性来解决这个问题。首先，我们必须使用以下命令为Node-RED安装一些管理工具：
  ```bash
  # npm install -g node-red-admin
  ```
  在这个阶段可能会出现一些错误或警告，但现在可以忽略它们。接下来，我们使用以下命令为Node-RED编辑器登录创建用户/密码凭据对：
  ```bash
  # node-red-admin hash-pw
  ```
  该工具会提示我们输入想要使用的密码，然后打印出哈希值，我们可以将其复制并粘贴到`settings.js`文件的下一步中。

  让我们打开`settings`文件，查看靠近122行的代码块。我们将取消对该代码块的注释，并以明文形式添加一个用户名；密码是我们在上一步中复制的哈希值。我创建了两个用户，所以每个用户有不同的哈希值。对于一个名为`admin`的用户，我们授予完全访问权限；对于一个名为`guest`的用户，只授予只读访问权限。
  ```javascript
  // 保护Node-RED
  // -----------------
  // 要对Node-RED编辑器和管理API进行密码保护，可以使用以下属性。有关详细信息，请参阅http://nodered.org/docs/security.html。
  adminAuth: {
      type: "credentials",
      users: [
          {
              username: "admin",
              password: "$2a$08$NeGbPtKiHU4JCC.IyqGz3tG2PeV.W8As9NEa62F9HX.qGz3tEA79mm",
              permissions: "*"
          },
          {
              username: "guest",
              password: "$2a$08$Fg/yRxn8As9NEa6435SvdNeGbPtKiOe/IyqGz3tG2PeV.A.UvRaTIXe",
              permissions: "read"
          }
      ]
  },
  ```
  完成此更改并上传`settings`文件后，我们必须使用`node-red`命令重新启动程序，然后导航到`www.in24hrs.xyz:1880/admin`。这次，我们会看到一个要求输入凭据的登录页面，如图5-20所示。
  ![受保护的Node-RED编辑器登录](https://i.imgur.com/xxxxxx.png)
  *图5-20 受保护的Node-RED编辑器登录*

  当我们启动Node-RED时，它会在命令行中以详细输出模式运行，这基本上意味着在它运行时我们无法执行其他操作。我们可以使用一个简单的命令将Node-RED作为服务在后台运行：
  ```bash
  # node-red > node-red.log &
  ```
  通过这个命令，Node-RED的输出日志将被重定向到`node-red.log`文件，并且末尾的`&`告诉Ubuntu将该程序作为守护进程运行（即在后台运行）。

  ### 总结
  我们现在拥有一个完全可运行的云实例，它几乎具备了我们构建自己的物联网平台所需的所有基本要素。下一步是开始构建平台的基础架构，添加所需的服务模块。

  如前几章所述，消息代理是关键组件之一，因此了解其功能非常重要。在下一章中，我将详细阐述消息代理。 

## 第6章 消息代理

在第4章中，我们选择了MQTT作为消息传递协议，并打算使用Mosquitto程序作为消息代理。在本章中，我们将更深入地探讨MQTT，并涵盖以下内容：
- 发布/订阅范式的工作原理
- MQTT的各种特性和功能
- WebSocket
- 何时使用消息代理
- 如何在我们的云实例上安装和保护MQTT

让我们深入研究MQTT，以及消息代理模块在我们的物联网平台中所扮演的重要角色。
#### 什么是MQTT？
MQTT是一种客户端 - 服务器发布/订阅消息传输协议。它具有轻量级、开放、简单且易于实现的特点。这些特性使其非常适合在许多场景中使用，尤其适用于诸如机器对机器（M2M）和物联网（IoT）等受限环境，在这些环境中，对代码占用空间要求较小，并且/或者网络带宽十分宝贵（OASIS标准 - MQTT版本3.1.1，2015）。

MQTT是一种非常轻量级的二进制协议，由于数据包开销极小，因此很容易在受限设备上实现，这使其成为物联网设备（即 “事物”）的理想选择。
#### 发布和订阅范式
与使用客户端 - 服务器架构的HTTP交互类似，MQTT采用发布/订阅架构。作为其内在要求，它需要在中间设置一个消息代理。这实际上将发布者（或发送客户端）与订阅者（或接收客户端）解耦。此外，这也意味着我们可以有多个订阅者订阅同一发布的消息。

让我们通过一个简单的示例来了解发布和订阅范式的工作原理，如图6 - 1所示。有五个客户端（即 “事物”）通过互联网连接到消息代理，每个客户端都扮演着不同的角色：
- 客户端A和B是传感器设备，分别传输温度和湿度数据。
- 客户端C是一台联网风扇。
- 客户端D是一台笔记本电脑。
- 客户端E是一个移动应用程序。

由于客户端C、D和E需要根据A和B发布的信息采取行动，所以它们是温度和湿度信息（主题）的订阅者。传感器客户端A和B会定期在各自的通道（主题）上传输温度和湿度数据（有效载荷）。消息代理负责跟踪哪些客户端已连接以及它们订阅了哪些主题。

每当客户端A和B发布信息时，客户端C、D和E就会收到这些信息。

发布/订阅范式的优点在于，即使其中一个客户端出现故障，也不会导致系统崩溃。消息代理在多个层面上帮助解耦客户端，使它们无需同时与代理保持连接，也无需进行时间同步。

发布/订阅机制还使我们能够实现更好的消息路由和基于事件的操作。这对我们的物联网平台非常重要，因为它可以改善整体架构并使其更加清晰。架构的可扩展性是我们在平台上使用MQTT和发布/订阅的另一个重要原因。

作为独立的客户端，系统中的每个节点都可以将其信息贡献到更大的生态系统中。这意味着我们可以让风扇C根据客户端A和B的读数，以及图外其他温度传感器（例如天气预报或环境监测系统）的读数来采取行动。
#### 消息代理和MQTT的其他特性
一般来说，术语 “客户端” 指的是所有连接到MQTT消息代理或通过它进行通信的设备或应用程序。因此，客户端可以是信息的发布者、订阅者，或者既是发布者又是订阅者。由于MQTT是基于TCP/IP的协议，任何能够使用TCP/IP进行通信的设备都应该能够建立MQTT连接并成为客户端。在实际应用中，可能会存在一些限制因素，但该协议本身就是为在受限设备上使用而设计的。

我们可以将传感器和控制系统设备视为客户端；同样，其他计算机、移动电话或Web应用程序也可以作为客户端和/或传感器。所有这些以有意义的方式组合在一起，最终形成一个可用的解决方案。

另一方面，消息代理是这种通信的协调者或组织者。它的职责是接收来自所有客户端的消息，对其进行过滤，查找哪些客户端订阅了哪些主题，然后将消息重新分发给这些客户端。消息代理还负责维护各种连接参数，例如客户端会话详细信息、凭据、存储的（即保留的）消息数据库以及其他一些信息。

由于消息代理在发布/订阅通信系统中扮演着如此重要的角色，因此它应该具有高可用性、容错性，易于部署、监控和维护，并且易于与整个解决方案的其他多个方面进行集成。我们选择的代理Mosquitto满足所有这些标准。
#### 服务质量
在发布/订阅机制中有一个有趣的概念，称为QoS（Quality of Service，服务质量），它是MQTT通信中一个关键且非常有用的特性。尽管MQTT协议本身轻量级且简洁，但它使客户端能够与代理长时间保持稳定的连接，并确保所有发布或订阅的消息都能在一定程度上得到传递保证。这种保证通过MQTT消息的 “QoS” 来实现。

QoS是消息发送方和接收方之间的一种约定。需要注意的是，就发布/订阅机制而言，至少存在两个发送方和两个接收方，因为每条消息都要经过代理。因此，对于任何发送的消息，代理是第一个接收方，然后才是后续的接收方，反之亦然。所以，在MQTT环境中，QoS在这两个层面上起作用。

MQTT支持三种QoS级别：
- **QoS 0**：最多一次（也称为 “即发即弃”），意味着消息可能会丢失。
- **QoS 1**：至少一次（即可能会有重复消息）。
- **QoS 2**：恰好一次。

有时，订阅者的QoS级别可能与发布者的QoS级别不同。例如，发布者以精确发送一次（QoS 2）的意图发送消息，但如果订阅者对这种确定性要求不高，它可以以QoS 0级别订阅该消息。因此，当代理尝试将此消息传递给订阅者时，QoS会根据订阅者的要求进行降级。相反，如果订阅者要求的服务级别高于发布者，QoS也会升级到订阅者要求的级别。

QoS机制有助于选择合适的通信服务级别，以匹配客户端的运行环境。例如，如果客户端处于通信环境不太可靠的移动车辆中（通过3G连接），那么这些客户端可以选择更高的QoS级别以确保接收到通信内容。而在家庭中通过Wi-Fi连接的客户端，由于连接可靠性较高，可以选择较低的QoS级别。

一般来说，当连接可靠且丢失一条或多条消息不会造成任何问题时，应选择QoS 0。QoS 1通常是最佳选择，因为消息至少会被接收一次，并且如果接收到重复消息，可以在应用层进行处理。如果每条消息都至关重要，绝对不能跳过或遗漏，则强烈建议使用QoS 2。需要注意的是，我们始终可以在应用层实现逻辑来维护和提升通信的可靠性，而无需依赖协议，但协议层提供的支持无疑是一个额外的优势。
#### 保持连接周期
MQTT协议是全双工的，具有实时通信功能，这意味着通信双方都必须保持连接并处于活动状态。在发布/订阅机制中，代理和客户端在通信时应保持连接并处于活动状态。如果由于任何原因（如网络不可靠等）导致连接中断，双方都应该知道发生了这种情况并采取相应的行动。

然而，MQTT所基于的基本TCP协议在实际应用中并不一定能完全做到这一点（理论上应该可以），这就可能导致一种称为半开连接的情况。为了解决这种情况，引入了保持连接（keep alive）机制。

保持连接周期是指客户端发送完一个控制数据包到开始发送下一个控制数据包之间允许的最大时间间隔。客户端有责任确保发送控制数据包的时间间隔不超过保持连接的值。如果在没有发送任何其他控制数据包的情况下，客户端必须发送一个PINGREQ数据包。（OASIS标准 - MQTT版本3.1.1，2015）

简而言之，如果客户端和代理之间的数据传输时间间隔小于保持连接周期指定的时间，连接将保持活动且完全可验证。如果没有数据传输，客户端则会发送一个两字节的传输（以PINGREQ数据包的形式），让代理知道它仍然在线。如果客户端在该时间间隔内既没有发送保持连接数据包也没有发送任何数据，代理将关闭连接并发布客户端在连接时设置的遗嘱消息（last - will message）。

虽然较短的保持连接周期似乎对数据质量/时效性/准确性有益，但如果发送过多不必要的消息，可能会适得其反。因为这可能会导致代理、网络和其他资源被用于处理这些开销，从而可能会减慢消息处理速度。

注意，保持连接的最大间隔为18小时12分钟15秒，即65,535秒。如果将保持连接间隔设置为0，则此机制将被禁用。
#### 遗嘱消息
如前所述，当客户端未向代理发送保持连接数据包或任何数据时，连接将被关闭，代理会向所有订阅者发布一条特殊消息，称为遗嘱消息（last will and testament，LWT）。这使得所有其他订阅者和应用程序能够在客户端意外断开连接时做出相应的有意义的反应。

需要注意的是，“正常断开连接” 指的是任何设备或客户端通过适当的协议握手主动从消息代理断开连接。如果没有发生这种情况（即断开连接是由网络故障等外部因素导致的），代理会立即向所有订阅者发布LWT消息。

每当客户端建立新连接时，用于该连接的众多参数之一就是遗嘱消息。与客户端发布的任何其他消息一样，这是一条客户端消息，但有一个特殊之处，即除非客户端意外断开连接，否则它不会被发布。LWT是通知所有其他订阅者客户端断开连接并让它们做出相应反应的最佳方式。
#### 保留消息
保留消息与客户端发布的其他消息类似。保留标志被设置为1或true，这会指示代理保留该消息的副本，直到它被删除或替换。每当有订阅者订阅与保留消息主题匹配的主题或模式时，该消息会立即被传递。

需要注意的是，每个主题只有一条保留消息，这为保持所有设备和应用程序了解其他所有内容的当前状态提供了一种有趣的方式。图6 - 1展示了我们如何建立这样一种机制。

在图6 - 1中，客户端A（一个温度传感器）首次连接到代理时，设置遗嘱消息为 “offline”，主题为 “temp - sensor/status”，并将保留标志设置为true。这告诉代理存储此消息，直到被客户端覆盖或删除，并且仅在温度传感器意外断开连接时发布该消息。建立连接后，传感器立即在同一主题 “temp - sensor/status” 上发布另一条消息 “online”，并将保留标志设置为true。

现在，任何新订阅 “temp - sensor/status” 的客户端都会始终收到 “online” 作为响应。然而，如果温度传感器断开连接，代理会发布其LWT消息，所有订阅者会立即收到 “offline” 消息。这使得其他所有客户端都能随时了解温度传感器的在线状态。或者，如果传感器想要主动断开连接，它可以首先在同一主题上发布一条 “disconnected” 消息，向所有客户端表明它已主动从代理断开连接。

带有LWT的保留消息是建立一种始终保持每个客户端状态更新的有效机制。

要从代理的存储中删除保留消息，只需在同一主题上发布一条空消息即可。例如，如果 “temp - sensor/status” 发布一条长度为0字节的字符串，并将保留标志设置为true，现有消息值将被覆盖，不再被保留。
#### 最棒的部分：WebSocket
我们已经了解了MQTT在物联网通信中，尤其是在我们自己的物联网平台中的重要性和工作原理。因此，自然期望我们能够在使用该平台的基于浏览器的应用程序中利用这一功能。WebSocket使基于浏览器的应用程序能够使用所有MQTT功能，这在许多有趣且有用的实际应用场景中都非常实用。

然而，浏览器目前还不能直接与TCP通信，因此也无法直接与MQTT通信。我们需要利用浏览器现有的与WebSocket协议通信的能力。WebSocket是一种网络协议，它在浏览器和Web服务器之间提供双向通信。该协议于2011年标准化，几乎所有现代浏览器都提供内置支持。好处是WebSocket协议与MQTT一样，也是基于TCP的，这使得集成变得更加容易。

现代浏览器和HTML5引入了许多新功能，使我们能够构建出行为类似于桌面和移动应用程序的网站。渐进式Web应用程序（Progressive Web Apps，PWAs）就是一个典型的例子。浏览器几乎安装在所有设备上，如台式计算机、笔记本电脑、平板电脑或智能手机。

使用通用协议与异构设备和系统进行所有通信所带来的一致性，是在WebSocket上使用MQTT的最诱人的原因之一。在WebSocket上使用MQTT还使我们的应用程序能够通过实时双向通道进行通信。从实时多方聊天到实时监控和控制系统，应用场景数不胜数。在本书后面的内容中，一旦平台的大部分功能构建完成并准备就绪，我们将启用并实现基于WebSocket的MQTT通信。
#### 我们使用的是最佳的消息代理选项吗？
有几种开发具有全双工功能和各种支持特性的消息代理的选项，其中一些包括使用原始TCP套接字、原始UDP套接字、AMQP和CoAP。与MQTT相比，这些替代方案大多存在更多的限制和复杂性，而优势相对较少。这就是为什么MQTT成为构建我们自己的物联网平台时最合适、高效和方便的选择。

请记住，所有这些协议都可以共存，并且如果需要，我们可以在同一云实例上部署它们。这意味着在未来，如果你决定同时使用AMQP和MQTT，是可以实现集成的。更重要的是，我们可以通过额外的插件程序将这些通道连接起来，以便从应用程序和设备的角度实现无缝通信。虽然这超出了本书的范围，但确实是可行的。
#### 何时使用消息代理以及何时不使用
从根本上讲，MQTT是一种异步协议，因此能够实现具有轻量级系统负担的双工通信。它允许系统在低带宽和低功耗条件下运行。相比之下，HTTP和类似协议需要相对较高的带宽和功率，并且本质上是请求 - 响应式的，这意味着客户端必须始终发起通信。

在希望任何一方（服务器或客户端）都能发起通信的场景中，MQTT是最佳选择。此外，如果系统需要在低数据消耗（特别是依靠电池供电）的情况下长时间运行，使用MQTT是明智的选择。如果设备需要频繁且随机地发送或接收数据，那么MQTT也很合适，因为它可以显著减少HTTP的开销。

如果带宽和功率不是问题，那么HTTP可能是更好的选择。另外，当数据发送或接收的频率不高时，使用HTTP也可以，因为在这种情况下，HTTP不会很快阻塞资源。

在需要实时控制或监控的应用中，MQTT显然是首选，因为它提供双工和双向通信能力，且开销最小。

你必须注意，基于MQTT的系统的工作量可能会呈抛物线式增长，这意味着对于每个添加到使用MQTT进行通信的网络中的设备（假设网络中总共有n个设备），系统的负载将变为n的平方（n * n）。图6 - 2以图形方式解释了这个概念。

例如，假设一个极端情况，有两个客户端，每个客户端都订阅了所有可能的主题（通配符#）。当一个客户端发布一条消息时，代理需要接收该消息，并且另一个客户端也需要接收该消息。这意味着发送一条消息可能会导致两次传输。对于另一个客户端也是如此，因此对于一个包含两个客户端的系统，总共会产生四条消息。对于一个包含三个客户端的系统，这个数字会变为九条消息（即每个客户端三条消息）。仅仅连接100个设备就意味着消息代理应该能够处理10 * 10（即100条消息），以此类推。

这意味着当客户端数量开始增加时，消息代理和整个系统（以及平台）的负载将几乎呈指数级增长；在我们后期扩展物联网平台时，需要牢记这一点。
#### 安装消息代理
既然我们已经讨论了发布/订阅机制的基本原理以及代理的工作方式，现在让我们在云实例上安装和配置MQTT代理。我们将在系统上安装Mosquitto代理，Ubuntu软件源中提供了最新版本的Mosquitto，可以通过apt软件包管理器进行安装。
```bash
# apt update
# apt install mosquitto mosquitto-clients
```
第一个命令更新软件包列表，第二个命令安装Mosquitto代理和本地客户端软件包。安装完成后，Mosquitto会立即启动，我们可以使用以下命令进行检查：
```bash
# lsof -i :1883
```
输出：
```
COMMAND  PID     USER   FD   TYPE  DEVICE SIZE/OFF NODE NAME
mosquitto 352 mosquitto    3u  IPv4 1131147      0t0  TCP  localhost:1883 (LISTEN)
```
默认情况下，Mosquitto监听标准MQTT端口1883，从输出中可以看到程序正在运行并监听该端口。此时，我们可以进行一个快速检查，看看一切是否正常。为此，我们打开另一个终端窗口并再次登录。让我们将两个窗口并排打开，以便观察情况。

在一个终端中，我们使用以下命令订阅testTopic：
```bash
# mosquitto_sub -h localhost -t testTopic
```
这里，-h指定主机名，-t指定订阅主题。发送命令后，我们不会看到任何输出，因为我们刚刚创建的客户端正在监听新消息，而此时还没有消息发送过来。

在另一个终端中，我们在testTopic上发布一条测试消息：
```bash
# mosquitto_pub -h localhost -t testTopic -m "Hello world !"
```
额外的 -m选项用于指定给定主题的消息内容。发送命令后，你应该会在另一个订阅了testTopic的终端上看到这条消息弹出。这确认了Mosquitto代理在我们的云实例上运行正常。

现在我们可以退出其中一个终端并停止订阅命令，继续进行后续设置。按Ctrl + C停止订阅命令。接下来，我们通过设置用户名和密码来保护Mosquitto代理。
#### 保护消息代理
Mosquitto安装包中包含一个实用工具，用于生成一个名为mosquitto_passwd的特殊密码文件。该文件会将用户名和密码的组合以哈希且不可读的格式存储在/etc/mosquitto/passwd中。要为用户名为anand的用户生成密码，可以使用以下命令：
```bash
# mosquitto_passwd -c /etc/mosquitto/passwd anand
# mosquitto_passwd /etc/mosquitto/passwd guest
```
执行上述命令后，系统会提示输入密码，密码随后会存储在密码文件中。命令中的 -c选项用于告诉程序在密码文件不存在时创建一个新文件，该选项仅在第一次使用时使用。第二条命令则是为另一个用户guest添加密码。

为使更改生效，我们需要将这些详细信息添加到Mosquitto的配置中，然后重启Mosquitto代理。首先，我们打开配置文件并添加相关内容，然后重启代理。
```bash
# nano /etc/mosquitto/conf.d/broker.conf
```
在文件中添加以下内容：
```bash
allow_anonymous false
password_file /etc/mosquitto/passwd
```
```bash
# lsof -i :1883
```
输出：
```
COMMAND  PID     USER   FD   TYPE  DEVICE SIZE/OFF NODE NAME
mosquitto 352 mosquitto    3u  IPv4 1131147      0t0  TCP  localhost:1883 (LISTEN)
```
```bash
# kill 352
```
在第一个命令中，我们打开一个新文件并添加两行内容。第一行设置配置，禁止任何匿名客户端/用户连接代理，只有在mosquitto_passwd文件中列出的用户才能连接。第二行指定密码文件的位置，以便代理可以对每个连接请求进行身份验证，代理会使用该文件中的用户名 - 密码组合。

为使更改生效，我们需要重启代理，但这次我们还需要指定刚刚创建的配置文件。所以首先，我们通过杀死正在运行的Mosquitto进程来停止服务。我们分两步完成：首先，通过lsof命令找到进程ID（即PID），然后向该PID发送kill信号。

接下来，我们使用配置文件重新启动代理，命令如下：
```bash
# mosquitto -c /etc/mosquitto/conf.d/broker.conf -v &
```
在该命令中，-c指定要加载的配置文件，-v指定输出详细信息，&告诉系统将代理作为守护进程在后台运行（即不阻塞终端）。

更改会在我们重启代理后很快生效。现在，如果你尝试按照之前的命令发布任何消息，它将无法工作，并且我们会收到一个错误：“Connection Refused: not authorised. Error: The connection was refused.”（连接被拒绝：未授权。错误：连接被拒绝。）

除了监听默认端口1883，Mosquitto还可以使用另一个端口8883进行安全通信。该端口为代理添加了基于TLS的连接。我们已经使用SSL证书保护了我们的域名（www.in24hrs.xyz），可以使用相同的证书来保护Mosquitto。此外，让我们通过重新打开配置文件broker.conf并添加以下内容来进一步配置：
```bash
# nano /etc/mosquitto/conf.d/broker.conf
```
在文件中添加以下内容：
```bash
# 启用日志记录
log_type all
log_timestamp true
log_dest stdout
log_dest topic
sys_interval 15
# 每15分钟保存一次日志
connection_messages true
autosave_interval 900
```
这些行告诉代理记录所有活动，包括新连接、订阅、发布、断开连接、错误和警告。这从调试的角度来看非常有用，同时也有助于分析代理的工作情况以及在运行过程中发生的事情。注意，在配置文件中，以#开头的行是注释。

我们还启用了连接消息的日志记录，这样每个新的客户端连接请求都会被记录下来，包括未授权和被拒绝的客户端。这有助于了解是否有未授权的客户端尝试连接，或者授权客户端是否无法连接，并且它还会记录传入连接的IP地址。这些详细信息会按照autosave_interval指定的每900秒（15分钟）保存到一个日志文件中。
```bash
# 启用会话持久化
persistence true
persistence_file broker_log.txt
persistence_location /var/lib/mosquitto/
```
我们启用了持久化功能，持久化会话的详细信息将存储在broker_log.txt文件中，该文件会保存在persistence_location指定的目录中。
```bash
# 禁止匿名客户端
allow_anonymous false
password_file /etc/mosquitto/passwd
```
我们再次添加这两行以确保禁止所有匿名连接，并提供用于身份验证的密码文件。
```bash
# 仅为本地主机开放安全端口
listener 1883 localhost
# 使用我们的SSL证书监听安全连接
listener 8883
certfile /etc/letsencrypt/live/in24hrs.xyz/cert.pem
cafile /etc/letsencrypt/live/in24hrs.xyz/chain.pem
keyfile /etc/letsencrypt/live/in24hrs.xyz/privkey.pem
```
在这里，我们在配置中添加了两个MQTT监听器。第一个监听器在端口1883上，这是标准的未加密MQTT端口。我们将此端口设置为仅绑定到本地主机，这样Mosquitto将不会对外公开访问，此端口可用于任何本地应用程序通过MQTT进行通信，而不会有任何麻烦。

第二个监听器设置在端口8883上，这是标准的加密MQTT端口，通常称为MQTTS（表示MQTT Secure）。声明后的三行指定了证书文件的位置，这些位置是我们在第5章为域名安装SSL证书时获得的。

我们保存配置文件并通过杀死旧进程来重启代理。
```bash
# lsof -i :1883
```
输出：
```
COMMAND  PID     USER   FD   TYPE  DEVICE SIZE/OFF NODE NAME
mosquitto 831 mosquitto    3u  IPv4 1131147      0t0  TCP  localhost:1883 (LISTEN)
```
```bash
# kill 831
# mosquitto -c /etc/mosquitto/conf.d/broker.conf -v > mosquitto.log &
```
上述命令将输出重定向到mosquitto.log文件，这样我们可以随时通过以下命令查看日志：
```bash
# cat mosquitto.log
```
或者使用以下命令实时监控日志：
```bash
# tail -f mosquitto.log
```
现在我们启用了另一个用于安全MQTT通信的新端口，还需要通过防火墙启用它。
```bash
# ufw allow 8883
```
输出：
```
Rule added
Rule added (v6)
```
一个完整修改后的broker.conf文件可以从GitHub仓库下载。
#### 总结
在本章中，我们详细讨论了消息代理，并为我们自己的物联网平台安装和测试了一个。在这个阶段，我们拥有一个完全运行的云实例以及MQTT消息代理，它将为我们的物联网平台提供强大且安全的实时消息代理服务。

在下一章中，我们将为平台构建一些关键组件。 

## 第7章 构建关键组件
我们带着明确的目标开启了物联网平台的构建之旅，即首先设想出我们自己的物联网平台的模样及其主要内容（参考第3章中的图3 - 1）。到目前为止，我们已经搭建好了一个功能完备的云实例和消息代理。

接下来的合理步骤是通过添加所需的服务模块，继续构建平台的其他基础架构。在本章中，我们将：
- 创建一个时间序列数据库
- 在Node - RED中更新额外的节点
- 创建一个数据库监听器
- 构建一个基于REST API的消息发布器和检索器
#### 创建时间序列核心数据库
我们知道时间序列数据库是物联网平台的关键模块之一。在前面的章节中，我们已经为其建立了通用的数据存储模式。现在我们将创建表。

打开浏览器，使用phpMyAdmin访问MySQL界面。第一步是在MySQL中创建一个单独的数据库，我们将其命名为tSeriesDB。使用phpMyAdmin界面创建新数据库的步骤如图7 - 1所示。

接下来，我们为数据库创建一个新的用户账户，并分配一个合适的密码，如图7 - 2所示。添加这个用户账户和访问权限意味着，每次你或程序想要访问时间序列数据库时，都需要这个密码。

现在，我们按照前面提到的模式添加一个数据表结构。要添加新表，在要创建表的数据库名称（在本例中为tSeriesDB）下选择“新建”选项。点击“新建”选项后，会出现一个对话框，用于输入新表名和多个列信息。

在这个阶段，我们可以添加更多或更少的列，之后也可以进行添加或删除操作，所以只需提供一个合适的表名即可继续。我们将使用thingData作为表名。

图7 - 3更详细地解释了数据表的创建步骤。注意，我们在模式中添加了一个列，该列的数据类型为二进制，名为deleted。添加这个列的目的是让我们能够将任何数据点标记为已删除，从而在进一步的计算中忽略它，同时又将其保留在数据库中。

当数据点仍然有效或处于活动状态时，我们可以将这个值标记为0（deleted = 0），当我们想要将其视为已删除时，将其标记为deleted = 1。如果我们决定从数据存储中完全删除这个值，那么可以根据需要删除这一行。这个设置为未来的应用程序增加了一层防错机制，避免应用程序误删数据。这有点类似于我们时间序列数据存储模块中的回收站。

因此，新采用的数据表模式如图7 - 4所示。
#### 在Node - RED中安装所需节点
MySQL中准备好所需的数据库和数据结构后，是时候在Node - RED中添加所需的额外节点，并对其进行配置，使其与数据库相连接。

Node - RED的默认安装中没有访问MySQL的节点，因此，我们将使用Node - RED的调色板管理器添加这个节点。

在浏览器中打开Node - RED界面，然后从右上角的菜单中选择“管理调色板”选项。这将打开“用户设置”页面，在其中输入MySQL，列出可用的节点。按照后续提示选择并安装node - red - node - MySQL，相关操作说明见图7 - 5。

有了这个节点，我们现在就可以从Node - RED访问我们的MySQL数据库了。此时，数据库和数据表已准备好接受和存储输入值。Node - RED功能实例已准备好进行配置和编程，并且MQTT消息代理也已正常运行。这些设置足以创建一个数据库监听器和相关的API。
#### 为我们的平台创建第一个流程
自安装Node - RED以来，我们尚未在节点环境中添加任何内容，因此画布是空的。一开始，我们将创建一个非常基本的流程。我们使用注入节点来实现这一目的，注入节点允许我们将消息注入到流程中，它可以是一个默认字符串，也可以是当前时间戳。消息可以通过点击节点上的按钮注入，或者我们也可以在节点配置中设置一个时间间隔，实现定期注入。

从输入调色板区域拖动一个注入节点到工作区，然后再拖动一个调试节点到工作区，将注入节点的输出连接到调试节点，从而将两个节点连接在一起。调试节点会将任何输入的内容发送到窗口右侧边栏的调试消息区域。

我们刚刚创建的序列仅存在于编辑器屏幕上，尚未激活。要激活这个序列，点击“部署”按钮。部署流程后，该序列即处于活动状态。如果你打开调试侧边栏，然后点击时间戳流程上的按钮，你会看到一个以UNIX微秒格式显示的输出（即当前时间戳）被发送到调试输出中。这是一个简单的基础流程，如图7 - 6所示，该图还展示了一些关于Node - RED编辑器的信息。

注意：有关如何使用Node - RED编辑器、键盘快捷键和各种术语的完整详细信息，请参考https://nodered.org/docs/user - guide/editor/上的文档。

我们将对基本序列进行两处更改：目前注入节点在按钮按下时发送时间戳，我们将把它转换为每隔15秒自动注入一次的操作；然后，我们从输出调色板中添加另一个节点，即mqtt out节点到我们的流程序列中，并将时间戳注入节点的输出连接到它的输入。
#### 添加MQTT发布功能
现在，让我们在Node - RED中添加MQTT发布功能。mqtt out节点不能直接使用，需要进行配置。我们将提供消息代理的详细信息和凭据来完成设置。设置此序列的四个步骤见图7 - 7。在这个配置中，我们使用端口1883与代理连接，不过请记住，我们也启用了安全端口8883，如果需要也可以使用它。由于我们是在安装代理的同一服务器实例上添加此连接，所以目前使用1883端口即可。

完成所有必要设置后，流程序列如图7 - 8所示。部署此序列后，时间戳注入开始，并且每隔15秒重复一次。时间戳的输出被发送到两个节点：调试节点和mqtt out节点。调试节点会在调试侧边栏中显示注入的时间戳，而mqtt out节点会将这个时间戳以“timestamp”为主题，以当前UNIX微秒时间戳的值（例如1543677926496）推送到消息流中。

这个流程序列满足了第4章需求清单中的M1要求（发布当前时间戳）。我们的第一个流程序列实现了同样的功能，即每隔15秒将当前时间戳发布到消息流中。根据需求，你可以修改这个时间间隔以满足你的期望，你可以将其降低到每秒发布一次（不过在我看来这有些过于频繁），或者提高到每小时一次，在大多数情况下这是合理的。
#### REST API消息发布器
既然我们已经了解了如何从Node - RED节点将消息发布到MQTT消息流中，现在我们将构建一个REST API来实现相同的功能。我们的D4需求的一部分是设备或应用程序应该能够使用HTTP协议发布消息。

首先，从输入模块中拖动HTTP节点到工作区。HTTP节点使我们能够处理来自Web的请求。将其放置在工作区后，双击该节点并修改节点设置，如图7 - 9所示。这里我们创建了一个端点“/pub”，并在POST请求中传递两个参数：topic和payload。我们可以在后续节点中通过消息对象msg.req.params.topic和msg.req.params.payload来访问这些参数。

再添加一个mqtt out节点，并如图7 - 9所示调整其设置。请注意，由于我们在创建时间戳实用工具时已经添加了代理配置，所以这里可以直接使用相同的配置。我们在设置中不指定主题，因为我们将在发布前的函数节点中提供它。

现在从函数调色板中拖动两个函数节点并连接它们，如图7 - 10所示。在这个序列的末尾添加HTTP响应节点，它将把提供的有效负载作为API响应输出。请记住，每个HTTP输入节点都必须有一个HTTP响应节点，否则API请求将永远不会结束，可能会导致用户端超时。

我们不对HTTP输出节点进行任何配置或设置更改。通常，只有在需要发送和配置额外的头信息或更改HTTP响应代码时，才会对HTTP输出节点进行配置。不过，HTTP输出的头信息和响应代码都可以在前一个函数节点中进行更改。

在这个流程序列中，两个函数节点中都编写了代码，下面对这些代码进行解释。
```javascript
// 创建消息
msg.topic = msg.req.params.topic;
msg.payload = msg.req.params.payload;
msg.qos = 2;
msg.retain = false;
return msg;
```
在“创建消息”函数块中，我们从HTTP节点接收输入。HTTP请求中传递的两个参数可以在msg消息对象中访问。在前两行中，我们将输入参数分配给主消息对象中的topic和payload。我们还将服务质量（QoS）设置为2以提高可靠性，并将保留标志设置为false，因为我们不希望每条消息都被保留。

这些输入被传递到mqtt out节点，该节点随后会在指定的主题下发布给定的消息有效负载，并将QoS设置为2，且不在代理上保留该消息。同时，我们通过创建一个响应并使用HTTP响应节点将其发送来响应该API调用。
```javascript
// 创建响应
msg.payload = {
    success: true,
    message: "published " +
        msg.req.params.topic +
        "/" +
        msg.req.params.payload
};
return msg;
```
由于HTTP响应节点会输出（即使用消息对象的有效负载内容响应调用的API），我们用两个键修改有效负载。设置success = true表示发布成功，并在有效负载中添加有意义的响应消息。

设置并更新完这些内容后，部署流程使其生效。如果一切正确，流程将成功部署，此时这个API就可以使用了。我们可以使用cURL在Node - RED端点上测试该功能，如下所示：
```bash
# curl -X POST "https://www.in24hrs.xyz:1880/pub/myTopic/myPayload" -i
```
输出：
```
HTTP/1.1 200 OK
Server: Apache
X - Powered - By: Express
Access - Control - Allow - Origin: *
Content - Type: application/json; charset=utf - 8
Content - Length: 56
ETag: W/"38 - O0I0tXOkbEG/goFLAbvDMSnHdqE"
{"success":true,"message":"published myTopic/myPayload"}
```
由于我们创建的是一个POST API，因此无法直接在Web浏览器中测试此功能。如果你希望直接在Web浏览器中使用它，只需通过更改HTTP输入节点的设置将其转换为GET请求即可。或者，你也可以使用任何其他工具（如Postman界面）来测试此功能。

现在我们为物联网平台添加了两个功能：一是定期将当前时间戳发布到MQTT消息流中；二是通过REST API将消息发布到同一MQTT消息流中。让我们通过添加数据库监听器来进一步增强此功能。
#### 创建数据库监听器
数据库监听器本质上是一种机制，通过某个程序或函数监听实时消息流，并将监听到的所有内容存储到数据库中。在我们的场景中，我们已经通过MQTT建立了一个实时消息流。现在我们将构建一个功能，使我们的程序流程能够监听MQTT流，并将所有消息记录到时间序列数据库中。

为此，我们从输入调色板中将mqtt输入节点添加到工作区，然后添加调试节点并将其连接到mqtt输入节点。这是最简单的流程序列，因为实际上我们只有一项需要配置。注意，MQTT代理的详细信息已经添加过了。

在mqtt输入节点中，我们需要提供订阅信息并将其设置为对应的代理。为此，双击mqtt输入节点并进行配置，如图7 - 11所示。我们订阅所有消息，订阅主题为“#”，并将QoS设置为2以确保可靠订阅。配置完成后，部署流程并在侧边栏中监控调试消息。

我们已经有一个活动的时间戳发布器，它每隔15秒发布一次当前时间戳，这些消息应该每隔15秒出现在调试侧边栏中。

现在，如果我们使用“/pub”API发布任何新消息，该消息也会出现在调试输出中。确认这一点后，让我们通过从函数调色板中添加一个函数节点和从存储调色板中添加一个MySQL节点来修改相同的流程序列。连接这些节点并配置MySQL节点的设置，如图7 - 12所示。

在“创建查询”函数中编写的代码如下所示：
```javascript
// 创建查询
// 获取微时间
var timestamp = new Date().getTime()/1000;
// 用尾随零填充
timestamp = timestamp.toString() + "000";
timestamp = timestamp.substring(0, 14); // 修剪为精确长度10 + 1 + 3
var strQuery = "INSERT INTO thingData (topic, payload, timestamp, deleted) VALUES ('" + escape(msg.payload) + "','" + timestamp + "', 0);";
msg.topic = strQuery;
return msg;
```
在代码的前三行中，我们从日期对象获取最新的时间戳，并将其转换为带零填充的字符串以便存储。第四行代码是我们编写的数据插入查询，它遵循标准的MySQL插入语法。

Node - RED中的MySQL节点要求将查询作为msg对象中的msg.topic传递。倒数第二行代码进行了这个赋值操作，然后函数将修改后的对象返回给MySQL节点。MySQL节点执行该查询并将记录添加到数据库中。

部署这个流程后，我们可以使用cURL发布任何消息，或者只需等待15秒让时间戳发布。然后登录到phpMyAdmin，在数据库中验证是否添加了新记录。

有了这个活动的流程，从现在开始，任何发布在MQTT消息流上的消息都会被记录在数据库中，我们的数据库监听器现在已正常工作。
#### REST API消息检索器
现在让我们创建一个API来检索存储在数据库中的消息。在我们的平台需求清单中，我们列出了两个相关需求：
- D1. 获取单个数据记录。使应用程序和设备能够根据指定的主题或主题模式从时间序列数据存储中查询单个数据记录。
- D2. 获取多个数据记录。使应用程序和设备能够根据指定的主题或主题模式查询多个数据记录。

这两个API的构建方式与前面的类似，但这次我们将使用MySQL节点通过SELECT SQL命令来访问和检索数据库值。相关设置配置和流程序列见图7 - 13。

我们将两个HTTP输入节点的输出连接到相同的流程序列。通过这样做，我们可以处理“/get/:topic”和“/get/:topic/last/:count”这两种不同的请求。第一个请求从时间序列数据库中仅检索一条消息，而第二个请求指定要检索的最新消息数量。

下面的代码片段展示了“创建查询”函数块中编写的代码：
```javascript
// 创建查询
if(!msg.req.params.count) // 如果未指定所需的记录数
    // 设置默认值为1
    msg.req.params.count = 1;
msg.topic = "SELECT id,topic,payload,timestamp " + // 构建SQL查询
    "FROM thingData " +
    "WHERE topic='" + escape(msg.req.params.topic) + "' " +
    "AND deleted=0 " +
    "ORDER BY id DESC " +
    "LIMIT " + msg.req.params.count + ";";
return msg;
```
在这段代码中，前两行检查是否存在参数count。请注意，只有在我们请求多个最新消息时才需要这个参数，因此在单个消息查询中，这个参数是不存在的。如果它不存在，我们将其设置为默认值1。

然后，我们使用标准的SELECT查询来检索数据库记录。在这个查询中，我们使用WHERE子句搜索指定的主题，并使用deleted = 0来仅选择未被删除的记录。此外，我们使用ORDER BY id DESC来检索最新的值，并使用LIMIT根据count参数限制输出数量。

由于这是一个时间序列数据库，并且根据我们构建数据库监听器的方式，所有值始终按时间顺序排列（如果按ID降序排序，最新的值在顶部）。现在让我们首先使用cURL检查这两个API：
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/myTopic" -i
```
输出1：
```
HTTP/1.1 200 OK
Server: Apache
X - Powered - By: Express
Access - Control - Allow - Origin: *
Content - Type: application/json; charset=utf - 8
Content - Length: 79
ETag: W/"38 - O0I0tXOkbEG/goFLAbvDMSnHdqE"
[{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/myTopic/last/3"
```
输出2：
```
[{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"},{"id":7,"topic":"myTopic","payload":"myPayload","timestamp":"1543716966.189"},{"id":6,"topic":"myTopic","payload":"myPayload","timestamp":"1543717132.192"}]
```
由于这些是GET API端点，你也可以直接在浏览器中测试它们。如果数据点不足（例如，你在查询某个主题下的五个数据点，而存储中只有三个），API将只响应可用的三个数据点。如何处理这种请求将取决于下游应用程序的逻辑。
#### 验证一切是否按预期工作
我们在构建过程中对四个核心服务和实用工具进行了测试，包括定期发布当前时间戳的功能、数据库监听器、HTTP数据发布服务以及HTTP数据检索服务。

在这个阶段，如果我们在MQTT消息流中发布任何内容，它将被实时发布，同时数据库监听器会将其记录到数据库中。如果通过HTTP发布服务发送任何消息，已连接到MQTT的应用程序和设备（如果它们订阅了该主题）将能够实时看到该消息。同时，这条消息也会被记录到数据库中。你可以随时从时间序列数据存储中检索一条或多条消息，而无需依赖实时连接。所有这些都可以通过命令行和网络浏览器进行测试。
#### 在后台持续运行Node-RED
一个主要的问题是，如果程序崩溃，我们必须登录到云实例并重新启动它。这不仅麻烦，更重要的是，首先要发现程序崩溃的情况也很困难。因此，在后台运行服务并在其出现故障或崩溃时自动重启就显得尤为重要。

有很多方法可以实现这一点。对于我们这种设置，我们将使用一个基于Node.js的实用工具——forever，它非常适合我们的需求。

forever实用工具是一个简单的命令行工具，它可以确保基于Node.js的应用程序持续运行（即“永远”运行）。这意味着，如果你的应用程序遇到错误并崩溃，forever会处理这个问题并为你重新启动它。

forever的安装非常简单，可以使用以下命令完成：
```bash
# npm install forever -g
```
安装完成后，我们就可以使用它重新启动Node-RED。如果此时Node-RED已经在运行，你可以使用以下命令在命令行中停止它：
```bash
# lsof -i :1880
```
输出：
```
COMMAND  PID     USER   FD   TYPE  DEVICE SIZE/OFF NODE NAME
node-red 1363 root    10u  IPv4 23374      0t0  TCP  *:1880 (LISTEN)
```
```bash
# kill 1363
```
我们看到Node-RED进程的ID是1363，使用kill命令停止了该进程。现在，我们使用forever实用工具来运行Node-RED，命令如下：
```bash
# forever start -l node-red.log --append /usr/local/bin/node-red
```
在上述命令中，我们使用一个名为node-red.log的日志文件来运行Node-RED，这意味着程序在控制台的所有输出都会写入这个文件，我们可以根据需要查看这个文件。

现在，你可以再次验证它是否在运行，并且我们的核心API是否按预期工作，这可以通过命令行和浏览器测试来完成。图7-14展示了我们物联网平台中现在已经正常工作的部分功能模块。
#### 总结
从我们最初几章的设想来看，我们已经完成了大约一半的工作。我们创建了时间序列数据库，并在平台中添加了数据库监听器。我们还在REST API接口模块中为平台创建了两个关键组件。

在下一章中，我们将为消息代理添加WebSocket功能，并更新访问控制以增强安全性。我们还将看到一些示例，展示我们的物联网平台如何通过MQTT套接字连接与其他应用程序和系统进行实时数据交换。 

## 第8章 配置消息代理
到目前为止，消息代理和云实例都处于正常运行状态。我们通过数据访问API为物联网平台增添了核心功能。在本章中，我们将修改MQTT代理的配置，使其更具实用性，特别是从互操作性的角度出发。在本章中，我们将：
- 了解WebSocket与普通MQTT之间的区别
- 理解WebSocket为何重要
- 为代理添加WebSocket功能并进行测试
- 添加访问级别控制
#### WebSocket与普通MQTT的区别
WebSocket提供了一个始终保持打开的通信通道，这与普通HTTP不同，HTTP的通道会在每次请求时打开和关闭。WebSocket提供了双向通信通道，但它不一定遵循MQTT协议。你可以实现一个原始的WebSocket，并让两个设备通过它进行通信。

在WebSocket之上使用MQTT增加了强大的功能。它使WebSocket客户端能够通过消息订阅选择自己想要接收的内容。它还增加了一种能力，即客户端可以通过代理向其他多个客户端发布消息或信息，而在正常情况下，使用基本的WebSocket通信来实现这一点会很繁琐。除此之外，MQTT的其他特性，如保留消息、服务质量（QoS）、遗嘱消息（LWT）等，使其更加出色。
#### WebSocket为何重要？
从我们自己的物联网平台角度来看，提供多种将平台连接到各种设备的方法是一项基本要求。这就是为什么我们选择了HTTP REST接口和MQTT来实现这一目的。然而，由于这两种协议和技术都有各自的优缺点，能够将它们结合起来会显著提升应用架构。

想象一下，如果你的Web应用程序能够与所有其他应用程序用户以及连接到平台的设备进行实时通信，它将获得多大的优势。通过使Web应用程序能够直接从浏览器控制这些设备，WebSocket为Web应用程序带来的强大功能，就是在MQTT上启用WebSocket的充分理由。

此外，所有系统和设备都可以使用MQTT和/或HTTP进行通信，并从我们集成到物联网平台的所有功能中受益。
#### 为MQTT配置添加WebSocket
为我们的代理添加WebSocket支持是一个简单的过程。在关于消息代理的章节中，你已经了解了如何添加监听不同端口的方法。我们之前添加了本地端口和安全端口用于通信。现在，我们将在配置文件中添加另一个监听端口定义，如下所示：
```bash
# nano /etc/mosquitto/conf.d/broker.conf
```
使用以下内容更新此文件：
```bash
# 仅为本地主机开放安全端口
listener 1883 localhost
# 使用我们的SSL证书监听安全连接
listener 8883
certfile /etc/letsencrypt/live/in24hrs.xyz/cert.pem
cafile /etc/letsencrypt/live/in24hrs.xyz/chain.pem
keyfile /etc/letsencrypt/live/in24hrs.xyz/privkey.pem
# 监听安全的WebSocket
listener 8443
protocol websockets
certfile /etc/letsencrypt/live/in24hrs.xyz/cert.pem
keyfile /etc/letsencrypt/live/in24hrs.xyz/privkey.pem
cafile /etc/letsencrypt/live/in24hrs.xyz/fullchain.pem
require_certificate false
tls_version tlsv1.2
```
前两个部分与之前相同，分别添加了本地监听器和安全的MQTT端口。第三部分添加了一个监听端口8443，并在下行声明该端口遵循WebSocket协议。请注意，MQTT over WebSocket的端口号并没有标准化，所以我们选择了在大多数网络环境（甚至在公司防火墙后）都能正常工作的最接近的变体。

现在我们为MQTT over WebSocket启用了一个新端口，我们必须通过防火墙启用它，然后重启代理以使新设置生效。
```bash
# ufw allow 8443
```
输出：
```
Rule added
Rule added (v6)
```
```bash
# pkill mosquitto
# mosquitto -c /etc/mosquitto/mosquitto.conf -v
```
#### 测试WebSocket
现在我们可以使用Eclipse Foundation提供的一个基于浏览器的公开可用的MQTT客户端来检查WebSocket功能，该客户端可以在www.eclipse.org/paho/clients/js/utility/访问。

当我们打开这个链接时，会出现一个简单的界面，如图8-1所示。按照我们的消息代理的信息填写连接信息，然后点击“Connect”按钮。Paho浏览器客户端将连接到我们的代理，并且当代理运行时，你可以在命令行中看到新的传入连接日志。

现在订阅任何主题并尝试在该主题上发布消息，你应该能收到消息。你也可以使用我们的“/pub”API进行测试，这样如果你在这个API上发布内容，你会在界面上看到该消息。同样，如果你从这个页面发布内容，你可以在浏览器中通过我们的“/get”API检索到相同的消息。这个工具还允许你发布遗嘱消息和保留消息。总的来说，这个客户端工具可用于未来对平台和MQTT的一般测试。

此外，你可以从这个工具的下载部分下载JavaScript库文件（或者直接查看源代码并下载paho - mqtt.js）。这个文件是一个用于WebSocket的开源客户端实现，可以很容易地与我们的物联网平台结合，用于你自己的应用程序。
#### 添加用户访问控制
在配置MQTT代理时，我们禁止了匿名登录，通过强制提供用户名和密码的方式在一定程度上保护了它。然而，这并不能阻止合法用户窥探彼此的数据。此外，任何人都可以发布或接收任何内容，这是不可接受的。

这就是为什么MQTT有另一种配置设置，称为访问控制列表（ACL）。通过使用ACL，我们可以为每个用户或客户端控制对主题的访问。我们本质上希望只允许授权的发布者发布特定主题的消息，并允许授权的订阅者收听这些广播。

这些ACL更改完全在代理端进行管理，客户端对此无需操作。要在我们的代理上添加ACL，首先让我们在代理上启用它。我们将修改代理配置文件，该文件位于/etc/mosquitto/conf.d/broker.conf，在文件末尾添加以下行。如果你已经在命令行上运行代理，请在编辑文件前按Ctrl + C停止它。
```bash
acl_file /etc/mosquitto/conf.d/broker.acl
```
请注意，ACL文件的名称是任意的。添加完后，保存配置文件，并使用以下命令在该文件夹中创建实际的ACL文件：
```bash
# touch /etc/mosquitto/conf.d/broker.acl
```
一般来说，一个ACL文件遵循一定的顺序。首先是通用访问控制部分，然后是用户特定的控制，最后是基于模式的控制。我们首先在ACL文件中添加以下内容，然后再进行解释。
```bash
# nano /etc/mosquitto/conf.d/broker.acl
```
在文件中添加以下行：
```bash
# 通用设置
topic read timestamp/#
# 用户设置
user admin
topic readwrite #
# 作为用户的应用程序设置
user my_app_name
topic read timestamp/#
topic readwrite myapp/%c/#
# 模式设置
topic read timestamp/#
pattern readwrite users/%u/#
pattern write %c/up/#
pattern read %c/dn/#
```
请记住，如果一行以#开头，代理会将整行视为注释。在设置中，%c和%u用作通配符模式，分别代表客户端ID和用户名。

第一部分是通用设置。在这里，我们给予普通用户对timestamp及其子主题的读取权限。请记住，我们在平台上完全禁止了匿名连接，这意味着这部分设置实际上永远不会被使用。

第二部分定义了两个用户的设置。第一个用户名为admin，对所有主题（即“#”）具有读写权限。第二个用户名为my_app_name，对我们的timestamp主题具有读取权限。此外，对于myapp/%c/#主题，该用户具有完全的读写访问权限。这意味着任何以my_app_name用户名和唯一客户端ID（例如device_1或类似名称）连接的客户端，都能够在myapp/device_1/#上进行读写操作。通过这种方式，我们可以在myapp应用程序域内隔离所有客户端。在这里，任何连接的客户端都不能在定义的主题之外的其他主题上发布消息，并且只能订阅timestamp主题。

最后一部分定义了基于模式的设置。同样，我们定义所有没有上述用户名的客户端都能够订阅timestamp主题。

此外，我们允许所有客户端在由users/%u/#模式定义的用户特定主题上进行发布或订阅，允许他们在%c/up/#上进行发布，在%c/dn/#上进行订阅。这意味着如果一个ID为device_1的客户端以foo_bar用户名连接，那么它能够在users/foo_bar/#上进行发布和订阅，在device_1/up/#上进行发布，并在device_1/dn/#上进行订阅。任何其他不符合给定模式的发布或订阅请求都将不被允许。

用户名 - 密码组合可以被多个应用程序和/或设备重复使用；因此，对用户设置的任何更改都会影响到所有使用该组合的应用程序和设备。一般来说，客户端ID在连接到代理时必须是唯一的。因此，基于客户端ID的模式设置中的任何访问控制都仅适用于单个客户端。

一旦更新了ACL文件，保存并关闭它。然后使用mosquitto_passwd实用工具创建ACL文件中包含的所有用户配置文件。

请注意，主题名称、用户名和客户端名称通常是区分大小写的；不仅在ACL中如此，在整个MQTT范式中都是如此。

为了使ACL生效，我们必须重启代理。我们首先终止现有进程，然后使用以下命令重新启动：
```bash
# pkill mosquitto
# mosquitto -c /etc/mosquitto/mosquitto.conf -v &
```
#### 检查设置是否生效
在你的电脑上安装一个本地MQTT客户端来进行这个测试会很方便。有很多选项可供选择，包括MQTT - FX和MQTTlense。你可以根据你的本地机器/PC设置搜索合适的程序。

由于我们已经启用了WebSocket，你也可以使用基于WebSocket的客户端（如Paho工具）进行测试。

在未使用ACL时，我们可以自由地订阅和发布任何主题。如果启用了ACL，代理将遵循ACL中列出的访问规则。

当我们以admin用户名连接时，我们能够订阅和发布任何主题，因为我们给予了admin用户所有访问权限。然而，如果我们以my_app_name用户名连接并尝试发布任何消息，除非消息遵循ACL中允许的主题模式，否则代理将不会发布该消息。

当由于ACL限制而被拒绝时，客户端无法知道代理拒绝了其发布消息的请求。但是，我们可以在代理日志中看到相关信息，如图8-2所示。

注意：对于MQTT版本3.1.1，代理没有办法通知客户端发布失败或被拒绝。不过在MQTT版本5.0中，这种情况将会改变，客户端将能够知道代理没有发布他们的消息以及原因。

模式设置和通用设置是累加的。这意味着如果模式设置不允许某个操作，但通用设置允许，那么代理将允许该操作。

模式设置优先于用户设置。因此，如果模式设置允许客户端订阅或发布某个主题，那么无论用户设置如何，代理都会允许。

由于我们在连接到消息代理时强制要求用户名和密码，可能会出现以下几种情况：
- **情况1**
    - 通用设置 = 空白或已配置
    - 用户设置 = 空白
    - 模式设置 = 空白
    - 结果是所有主题的访问都被拒绝。
- **情况2**
    - 通用设置 = 空白或已配置
    - 用户设置 = 已配置
    - 模式设置 = 空白
    - 结果取决于用户设置，代理会忽略通用设置。
- **情况3**
    - 通用设置 = 空白或已配置
    - 用户设置 = 空白或已配置
    - 模式设置 = 已配置
    - 结果是模式设置优先于用户设置。
#### 在消息代理中使用Forever工具
我们正在使用forever实用工具来保持Node-RED持续运行。现在我们也用它来确保消息代理能持续运行。这样，如果代理出现故障，forever应用程序会将其重新启动。虽然forever可以轻松用于基于Node.js的应用程序，但要将其用于Mosquitto代理还需要一些额外的工作。

首先，我们创建一个调用mosquitto命令的Shell脚本，然后使用forever工具执行该文件。使用以下命令创建一个Shell脚本：
```bash
# nano mqtt-sh.sh
```
在文件中添加以下内容：
```bash
#!/bin/sh
/usr/sbin/mosquitto -c /etc/mosquitto/conf.d/broker.conf
```
保存此文件后，使其可执行，然后使用forever工具运行它（首先杀死之前的进程），命令如下：
```bash
# chmod +x mqtt-sh.sh
# pkill mosquitto
# forever start -l mqtt.log --append -c -sh /root/mqtt-sh.sh
```
这个命令会在后台启动我们的消息代理并使其持续运行。代理的所有输出都可以在mqtt.log文件中查看。要查看当前使用forever工具运行的应用程序列表，可以在命令行中输入forever list，它会列出所有正在运行的应用程序及其运行时间和其他信息。
#### 总结
现在我们拥有了一个功能完备的MQTT消息代理、部分功能可用的REST API、正常工作的时间序列存储以及一些实用功能。

在下一章中，我们将创建其余的REST API，并为平台添加消息路由器。我们还将为平台添加实用工具API，使其完全准备就绪。 

## 第9章 创建REST接口
既然我们已经为构建有意义的API奠定了基础，在本章中，我们将：
- 创建数据访问API
- 开发实用工具API
#### 数据访问API
作为需求清单的一部分，我们有标记为D1和D2的需求，主要用于从时间序列数据存储中访问数据记录。在第7章创建核心服务时，我们已经涉及了这部分内容。我们还涵盖了通过HTTP进行消息发布API的D4需求。

接下来我们要创建的API，将帮助我们根据指定的主题或有效负载条件，或者两者结合，获取一条或多条数据记录。这个条件可以是主题或有效负载模式，也可以是与时间戳相关的，比如获取特定时间段内的数据。

我们所有的数据访问API都有相似的结构，每个API都包含一个HTTP输入节点、创建查询功能块、MySQL节点。MySQL查询的输出会经过准备响应功能节点，然后通过HTTP响应发送出去。

每个HTTP输入节点都有唯一的端点结构和指定的方法，可用于API调用以触发该流程序列。在创建查询节点中，我们将构建合适的查询来获取所需的数据，并将其传递给执行实际查询工作的MySQL节点。在准备响应节点中，目前我们没有做什么，但之后我们可以添加某种格式化操作，或者在响应JSON中添加更多对象，以进一步增强API的功能。图9-1展示了基于条件的数据访问API的样子。

这里我们的端点是“/get/topicLike/:topic/payloadLike/:payload/last/:count”，其中主题和有效负载输入是基于通配符的。由于我们要查询不确定数量的记录，所以必须添加count参数来指定请求的记录数量。

下面的代码片段展示了创建查询功能块的代码：
```javascript
// 创建查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
// API查询中使用的通配符是*，需要转换为SQL通配符%
msg.topic = "SELECT id,topic,payload,timestamp" +
    " FROM thingData WHERE" +
    " topic LIKE '" + msg.req.params.topic.replace(/\*/g, "%") + "'" +
    " AND" +
    " payload LIKE '" + msg.req.params.payload.replace(/\*/g, "%") + "'" +
    " AND deleted=0" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY ID DESC" +
    " LIMIT " + msg.req.params.count + ";";
return msg;
```
在上述代码中，前两行至关重要。虽然我们直到本章末尾才会实现身份验证过滤，但我们的代码已经为此做好了准备。我们检查authFilter对象是否可用，如果不可用（即其值未设置），我们暂时将其设置为默认值1。将其设置为1可确保在添加身份验证功能之前，我们的代码和API能够正常工作。在理想情况下，这个默认值应该设置为0，这样如果未设置authFilter，将不会返回任何数据。

在后续构建的查询中，我们将通配符*替换为实际的SQL通配符%。我们故意不在API查询中使用%，以避免其与上游应用程序中可能出现的HTML编码混淆。我们使用*作为通配符，因为大多数常见的主题或有效负载中都不会包含这个字符。

然而，如果你是为特定领域的应用构建物联网平台，可以根据需求轻松更改它。只需记住，在代码中使用该通配符的所有地方都要进行相应修改。

在查询的其余部分，我们还会检查未删除的记录。注意我们添加的与authFilter相关的条件。在未提供authFilter的情况下，默认值为1。在SQL意义上，这个条件部分“AND (1)” 就意味着true，因此不会改变查询的其余部分。如果该值变为0，这个条件就变成“AND (0)”，在SQL中意味着false。这将否定其余条件，从而不会有输出。

需要注意的是，authFilter不一定只包含1或0，它完全可以包含另一个SQL条件，然后与查询的其余部分结合，以提供有意义的输出。在后续阶段，我们将用基于身份验证的查询模式替换authFilter。

现在这个查询将生成所有符合我们标准的记录，并根据我们请求的记录数量限制输出，然后以JSON响应的形式发送给API调用者。使用cURL调用这个API的示例如下：
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/topicLike/my*/payloadLike/*/last/5"
```
输出1：
```json
[{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543731089.784"},{"id":7,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"},{"id":18,"topic":"mytopic","payload":"myplayload","timestamp":"1543716966.189"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/topicLike/timesta*/payloadLike/*/last/2"
```
输出2：
```json
[{"id":31,"topic":"timestamp","payload":"1544011400243","timestamp":"1544011400.245"},{"id":30,"topic":"timestamp","payload":"1544011399074","timestamp":"1544011399.078"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/topicLike/timesta*/payloadLike/*88*/last/2"
```
输出3：
```json
[{"id":28,"topic":"timestamp","payload":"1544011288907","timestamp":"1544011288.910"}]
```
在输出3中可以看到，尽管我们在请求中指定了count为2，但响应中只有一条数据记录，因为可能只有一条记录符合我们的主题和有效负载模式要求。输出1的情况也是如此。
#### 添加基于时间的过滤器
除了基于模式的数据API，我们还将添加基于时间的API。这里我们创建三个端点：一个用于获取在指定时间戳之后创建的、指定主题或主题模式的数据记录；一个用于获取在指定时间戳之前创建的记录；还有一个用于获取在两个时间戳之间创建的记录。

这个功能的流程序列如图9-2所示。

我们有三个端点：
- `/get/:topic/after/:time/last/:count`：topic是要搜索的数据记录的主题名称。time参数是用于搜索条件的UNIX风格时间戳。我们将搜索在这个时间戳之后创建的记录。由于可能有多个记录满足这个条件，所以我们强制要求添加count参数来指定请求的记录数量。
- `/get/:topic/before/:time/last/:count`：与after查询的端点类似，但这里我们搜索在指定时间戳之前创建的记录。
- `/get/:topic/during/:start/:end/last/:count`：这里我们提供起始和结束时间戳，以搜索在这两个时间戳之间创建的记录。

相应地，根据使用的端点不同，创建查询功能块会略有不同，因为每个查询都不一样。下面是三个代码片段：
```javascript
// 创建“AFTER”查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
// API查询中使用的通配符是*，需要转换为SQL通配符%
msg.topic = "SELECT id,topic,payload,timestamp" +
    " FROM thingData WHERE" +
    " topic LIKE '" + msg.req.params.topic.replace(/\*/g, "%") + "'" +
    " AND" +
    " timestamp >= '" + msg.req.params.time + "'" +
    " AND deleted=0" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY ID DESC" +
    " LIMIT " + msg.req.params.count + ";";
return msg;
```
```javascript
// 创建“BEFORE”查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
// API查询中使用的通配符是*，需要转换为SQL通配符%
msg.topic = "SELECT id,topic,payload,timestamp" +
    " FROM thingData WHERE" +
    " topic LIKE '" + msg.req.params.topic.replace(/\*/g, "%") + "'" +
    " AND" +
    " timestamp <= '" + msg.req.params.time + "'" +
    " AND deleted=0" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY ID DESC" +
    " LIMIT " + msg.req.params.count + ";";
return msg;
```
```javascript
// 创建“DURING”查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
// API查询中使用的通配符是*，需要转换为SQL通配符%
msg.topic = "SELECT id,topic,payload,timestamp" +
    " FROM thingData WHERE" +
    " topic LIKE '" + msg.req.params.topic.replace(/\*/g, "%") + "'" +
    " AND" +
    " timestamp >= '" + msg.req.params.start + "'" +
    " AND" +
    " timestamp <= '" + msg.req.params.end + "'" +
    " AND deleted=0" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY ID DESC" +
    " LIMIT " + msg.req.params.count + ";";
return msg;
```
在上述三个代码片段中，除了查询中检查时间戳的部分不同，其他部分都是相同的。这些查询通过小于或大于条件来搜索时间戳，很容易理解。

我们使用一个通用的输出序列块，因为所有端点的性质相似。下面基于cURL的测试展示了如何使用这个API。请记住，你也可以在网络浏览器中测试这些API。
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/mytopic/last/7"
```
输出1：
```json
[{"id":18,"topic":"mytopic","payload":"myplayload","timestamp":"1543731089.784"},{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"},{"id":7,"topic":"myTopic","payload":"myPayload","timestamp":"1543716966.189"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/mytopic/before/1543717154.899/last/5"
```
输出2：
```json
[{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"},{"id":7,"topic":"myTopic","payload":"myPayload","timestamp":"1543716966.189"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/mytopic/after/1543717154.899/last/5"
```
输出3：
```json
[{"id":18,"topic":"mytopic","payload":"myplayload","timestamp":"1543731089.784"},{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/mytopic/during/1543717154/1543731089/last/5"
```
输出4：
```json
[{"id":8,"topic":"myTopic","payload":"myPayload","timestamp":"1543717154.899"}]
```
在上述测试输出中，第一个输出是列出mytopic主题下所有可用的记录。可以看到，实际上只有三条可用记录。接下来的三个输出展示了每个API的调用方式及其输出结果。
#### 数据删除API
我们将以两种截然不同的方式实现数据删除操作：一种可恢复，另一种不可恢复。对于可恢复删除，我们会利用数据表中的`deleted`字段。在之前的所有查询中，我们始终在条件中搜索`deleted = 0`的记录。现在，每当我们想要标记某条记录为已删除时，只需将`deleted`设为1即可，就是这么简单。

让我们创建一个满足以下需求的API：
- D5. 删除单个数据记录。
- D6. 按顺序删除多个数据记录。
- D7. 根据特定条件删除一个或多个记录。

此功能的流程序列如图9-3所示，我们创建了四个API端点。

第一个端点是`/delete/:topic/id/:id`，其中`topic`是我们要引用的主题名称，`id`是该主题记录中要删除的记录ID。你肯定注意到了，每次我们发布或请求数据时，返回的结果都包含`id`和内容，这里的`id`就是我们在API中所指的ID。下面的代码片段展示了这个端点的创建查询块：
```javascript
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "UPDATE thingData" +
    " SET deleted=1" +
    " WHERE" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ")" +
    " AND id=" + msg.req.params.id + ";";
return msg;
```
在上述代码片段中，注意我们是如何更新匹配给定主题和相关ID的记录，并将`deleted`标志设置为1的。请注意，我们将记录保留在时间序列数据存储中，如果需要，你可以编写另一个API以相同的方式取消删除记录，只需将`deleted`设置为0即可。

下面的代码片段展示了我们如何处理`/delete/:topic`API。它与前面的端点类似，但有一个区别：没有提供特定的记录ID。
```javascript
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "UPDATE thingData" +
    " SET deleted=1 " +
    " WHERE" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ");";
return msg;
```
在这段代码中，我们将给定主题的所有记录都标记为已删除，而不是只标记一条记录，这是一个批量删除API。这次，我们还将修改准备响应函数节点。对于除`SELECT`之外的查询，MySQL节点返回的值有所不同。我们并不需要所有这些值，因为只有两个值对我们的API直接有用。
```javascript
// 准备响应
msg.payload = {
    "found": msg.payload.affectedRows,
    "changed": msg.payload.changedRows
};
return msg;
```
在上述代码中，`affectedRows`是匹配查询条件的行数，而`changedRows`是被新数据更改的行数。现在我们用cURL测试这个API：
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/timestamp/last/5"
```
输出1：
```json
[{"id":36,"topic":"timestamp","payload":"1544188856526","timestamp":"1544188856.529"},{"id":35,"topic":"timestamp","payload":"1544188710842","timestamp":"1544188710.845"},{"id":34,"topic":"timestamp","payload":"1544188664410","timestamp":"1544188664.413"},{"id":33,"topic":"timestamp","payload":"1544188641076","timestamp":"1544188641.084"},{"id":32,"topic":"timestamp","payload":"1544177423967","timestamp":"1544177423.973"}]
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/delete/timestamp/id/34"
```
输出2：
```json
{"found":1,"changed":1}
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/get/timestamp/last/5"
```
输出3：
```json
[{"id":36,"topic":"timestamp","payload":"1544188856526","timestamp":"1544188856.529"},{"id":35,"topic":"timestamp","payload":"1544188710842","timestamp":"1544188710.845"},{"id":33,"topic":"timestamp","payload":"1544188641076","timestamp":"1544188641.084"},{"id":31,"topic":"timestamp","payload":"1544011400243","timestamp":"1544011400.245"},{"id":30,"topic":"timestamp","payload":"1544011399074","timestamp":"1544011399.078"}]
```
从上述三个命令中可以看到，第一个命令用于检查可用记录，注意我们有从ID 32到36的时间戳数据。在第二个命令中，我们删除了ID为34的记录。在第三个命令中，我们再次检查最后五条记录，在输出3中可以看到ID 34不再返回。

最后两个端点用于从记录集的开头或结尾删除指定数量的记录。API`/delete/:topic/last/:count`用于删除指定数量的最新数据记录，`/delete/:topic/first/:count`用于删除指定主题的记录集中从开头起指定数量的记录。
```javascript
// 如果未指定所需记录数量
// 设置默认值为1
if (!msg.req.params.count) {
    msg.req.params.count = 1;
}
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "DELETE thingData" +
    " WHERE deleted=1" +
    " AND" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY id DESC LIMIT " + msg.req.params.count + ";";
return msg;
```
上述代码片段用于删除指定数量的最新记录。另一个API的代码与之相同，但将排序关键字从`DESC`改为`ASC`以实现按升序删除。对于所有这些操作，你可以在时间序列数据存储中查看，这些记录仍然存在，只是`deleted`标志被设置为1。

如果要定期删除存储的旧数据，这两个API会很有用，比如删除开头几条记录的API可能会很方便。
#### 完全删除数据记录
如果我们想从时间序列数据存储中完全删除数据该怎么办呢？这就是我们创建另一组类似删除API的地方，我们可以称之为清除（purge）API。图9-4展示了创建的序列。

虽然端点的创建遵循相同的语法，但代码略有不同。下面是第一个端点的代码片段：
```javascript
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "DELETE thingData" +
    " WHERE deleted=1" +
    " AND" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ")" +
    " AND id=" + msg.req.params.id + ";";
return msg;
```
注意我们是如何使用`DELETE`查询的。这里我们检查指定的主题和ID，以及`deleted`状态是否已设置。这意味着如果记录未被标记为删除，我们就无法清除它。要从时间序列存储中完全删除一条数据记录，我们需要进行两个连续的操作：先删除，再清除。
```javascript
// 清除多条记录
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "DELETE thingData" +
    " WHERE deleted=1" +
    " AND" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ");";
return msg;
```
```javascript
// 清除开头几条记录
// 如果未指定所需记录数量
// 设置默认值为1
if (!msg.req.params.count) {
    msg.req.params.count = 1;
}
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "DELETE thingData" +
    " WHERE deleted=1" +
    " AND" +
    " topic='" + msg.req.params.topic + "'" +
    " AND (" + msg.req.authFilter + ")" +
    " ORDER BY id ASC LIMIT " + msg.req.params.count + ";";
return msg;
```
清除最后几条记录的查询与清除开头几条记录的查询类似，只是排序关键字需要从`ASC`改为`DESC`。

目前，我们已按照需求清单完成了数据API的构建。不过，仍有许多可改进的地方，这些改进可以在保持API简单直接的同时，进一步增强其功能。我们稍后会再审视这些方面。
#### 向平台添加微服务
微服务是应用程序和设备都能使用的非结构化功能。我们在平台需求清单中列出了七个微服务。现在我们将逐个构建它们。
#### 获取当前时间戳
我们在上一章构建了一个当前时间戳发布服务，满足了需求M1。现在我们将添加一个基于轮询的时间戳服务。这将是一个简单的API，如图9-5所示。对于那些错过最新时间戳广播且无法等待下一次广播的设备或应用程序来说，这个服务非常有用。有了这个微服务，你还可以强制应用程序或设备根据随机的时间戳请求更新其时钟。

这是我们物联网平台上最简单的API。它可以通过`/timestamp`端点访问。准备时间戳函数节点只有几行代码，如下所示：
```javascript
msg.payload = {
    timestamp: (new Date()).getTime().toString()
};
return msg;
```
我们创建一个新的日期对象，将其转换为UNIX风格的时间戳，然后格式化为字符串，再打包到消息对象中。
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/timestamp"
```
输出：
```json
{"timestamp":"1544201700375"}
```
使用cURL进行的快速命令行测试显示了UNIX风格的微秒时间戳输出。
#### 随机代码生成器
让我们创建另一个快速实用工具，用于生成指定长度的随机字母数字字符串。这对于创建令牌、默认密码或API密钥特别有用。图9-6展示了这个服务的序列。

准备随机代码函数节点的代码片段如下：
```javascript
var randomString = function(length) {
    var text = "";
    var possible = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789";
    for (var i = 0; i < length; i++) {
        text += possible.charAt(Math.floor(Math.random() * possible.length));
    }
    return text;
}
msg.payload = {
    code: randomString(msg.req.params.len)
};
return msg;
```
我们首先定义了一个函数，用于生成指定长度的随机字符串。函数定义之后，我们将这个函数的输出传递给消息对象，并作为输出发送出去。下面是这个API的命令行测试：
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/randomcode/32"
```
输出1：
```json
{"code":"ASQ6t9PidHZ0BKxIsQfguD72fp0DcErq"}
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/randomcode/8"
```
输出2：
```json
{"code":"rU5rB3Db"}
```
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/randomcode/64"
```
输出3：
```json
{"code":"bydGpfpth9xE4vy9HcM97s1Jm3dmMipB0UuJB2Lqn95pkPrMM4IdxtaxUEBLvR1A"}
```
可以看到，我们现在能够生成不同长度的随机字符串。实际上，对于可以生成的随机字符串的最大长度没有限制；不过，通常使用32字节或64字节，在某些情况下会使用128字节。这完全取决于具体的用例。
#### 向Node-RED添加新模块
在创建其余三个微服务之前，我们需要安装额外的Node-RED模块。这些节点分别是用于生成UUID的模块、用于邮件服务的Sendgrid模块以及用于短信服务的Twilio模块。

要安装额外的节点，在Node-RED界面的主菜单中选择“管理调色板”选项。点击“安装”选项卡，输入搜索词“contrib-uuid”。你会看到一个名为“node-red-contrib-uuid”的贡献包，如图9-7所示。按下“安装”按钮将该节点添加到节点调色板中。

我们以同样的方式搜索并安装“node-red-contrib-sendgrid”和“node-red-node-twilio”（见图9-7）。请注意，Node-RED默认有一个用于发送和接收电子邮件的节点。你也可以尝试使用那个节点，不过我发现它提供的定制性不足，并且由于邮件账户和客户端的安全限制，维护起来可能会很麻烦。而Sendgrid是一个自动化的电子邮件发送程序。
#### UUID生成器
现在我们已经安装了所有需要的节点，让我们创建一个UUID生成器微服务。UUID生成API的流程序列如图9-8所示，同时展示了UUID节点的设置。

我们将UUID节点设置为生成随机代码；不过，你也可以尝试基于特定的命名空间或时间戳来生成。当基于命名空间和时间戳生成UUID时，大部分内容会非常相似并遵循一定的模式。UUID节点以字符串格式将生成的代码输出到`msg.payload`中，我们在准备响应节点中将其转换为消息对象，如下所示：
```javascript
// 准备响应
msg.payload = {
    uuid: msg.payload
}
return msg;
```
让我们快速使用cURL检查这个API的功能，这里的端点是`/uuid`。
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/uuid"
```
输出：
```json
{"uuid":"a304fad2-36d1-4fda-9e2d-da820524ce6f"}
```
#### 电子邮件和短信微服务API
要使用Twilio和Sendgrid，你必须在它们各自的平台上注册账户。你可以在www.twilio.com上注册Twilio账户，在https://sendgrid.com上注册Sendgrid账户。创建账户后，你会获得各种凭证。请妥善记录这些凭证，因为首次配置这两个节点时会用到（见图9-9）。
#### 节点配置
首先，从Node调色板的左侧将两个节点拖到工作区。

将它们放置在工作区画布上后，双击sendgrid节点，并在其设置窗口中提供API密钥。这就是设置sendgrid节点所需做的全部操作。其他值，如发件人和收件人电子邮件地址，将在构建服务时提供。如果需要，你可以给这个节点命名，否则它将使用默认名称sendgrid。

现在双击twilio节点，然后点击铅笔图标创建一个Twilio配置。这将打开另一个对话框，需要输入三个重要信息。粘贴你在Twilio账户页面上提供的账户SID、Twilio号码和认证令牌。如果需要，你可以给这个配置指定一个名称，然后点击“添加”保存此配置。

此配置会单独保存，以后多个节点都可以使用。保存确认后，你会回到twilio节点设置对话框。从下拉菜单中选择这个配置（第一次只有一个选项）。不需要更改其他字段，因为我们稍后会通过编程方式进行更改。
#### 短信发送工具
配置设置完成后，让我们使用图9-10所示的流程序列创建一个短信API。

这里我们将端点定义为“/sms/to/:to/message/:message”，其中“to”是我们要发送短信的目标手机号码，“message”是短信的实际内容。

请注意，twilio节点没有输出，因此没有直接的方法知道我们的短信发送请求是否成功。由于Twilio本质上是异步的，它在账户设置中提供了回调功能。你可以指定一个回调URL，在消息成功发送或发送失败时会调用该URL。有趣的是，我们可以为此创建另一个API，并根据应用程序的需要处理所有的送达报告。我将这个探索的机会留给你，因为它并不复杂，而且也不是平台的关键部分。

相应地，有两个函数块进行处理。第一个函数准备通过twilio节点发送的消息，另一个函数准备HTTP API响应。下面是两个函数的代码：
```javascript
// 准备消息
msg.topic = msg.req.params.to;
msg.payload = msg.req.params.message;
return msg;
// 准备响应
msg.payload = {
    "smsTo": msg.topic,
    "message": msg.payload,
    "status": "queued"
};
return msg;
```
twilio节点需要在消息的主题部分包含目标号码，在有效负载部分包含消息内容。在准备消息函数节点中，我们根据这个要求准备消息包。

准备响应节点只是用“queued”状态响应API调用者，并回显发送给它的内容。部署此流程后，可以使用以下cURL命令进行检查，目标手机应该会在短信收件箱中收到一条消息（“Hi”）。
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/sms/to/+1234567890/message/Hi"
```
输出：
```json
{"smsTo":"+1234567890","message":"Hi","status":"queued"}
```
#### 电子邮件发送工具
现在我们的短信API按预期工作了，接下来我们将创建一个电子邮件发送功能的流程序列（见图9-11）。它与短信发送流程非常相似。

为了发送电子邮件消息，我们简化了API，其端点为“/email/to/:to/subject/:subject/message/:message”，参数含义一目了然。第一个参数“to”指定要发送电子邮件的目标地址，后面跟着电子邮件主题和消息内容参数“message”。

sendgrid节点要求在msg对象中提供输入，并且它没有任何输出。因此，我们添加一个准备响应函数来响应API调用者。
```javascript
// 准备电子邮件
msg.from = "in24hrs <the.author@in24hrs.the.book>";
msg.to = msg.req.params.to;
// msg.bcc = "bcc_address@example.com";
msg.topic = msg.req.params.subject;
// msg.cc = "cc_address@example.com";
msg.payload = msg.req.params.message;
return msg;
// 准备响应
msg.payload = {
    "to": msg.to,
    "status": "email queued"
};
return msg;
```

在准备电子邮件函数中，我们将发件人邮箱地址硬编码到msg.from中，你可以根据实际需求更改此设置。其他部分，如收件人地址（msg.to）、主题（msg.topic）和邮件内容（msg.payload），则从API调用中获取。

准备响应函数只是用“email queued”状态响应API调用者，并回显收件人地址。部署此流程后，可以使用以下CURL命令进行检查，目标邮箱应该会收到一封主题为“Hello”、内容为“World”的邮件。
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/email/to/someone@example.com/subject/Hello/message/World"
```
输出：
```json
{"to":"someone@example.com","status":"email queued"}
```
#### 加密实用工具
在构建平台时，我们可能需要对某些内容进行加密。Node - RED默认没有提供加密功能，因此我们将使用一个名为CryptoJS的JavaScript库。

首先，我们需要将这个库添加到Node - RED的工作目录中。Node - RED的工作目录通常是用户主目录下的.node - red文件夹。
```bash
# cd ~/.node - red
# wget https://cdnjs.cloudflare.com/ajax/libs/crypto - js/3.1.9 - 1/crypto - js.min.js
```
上述命令会将CryptoJS库下载到Node - RED的工作目录中。现在，我们可以在Node - RED中使用这个库了。

让我们创建一个简单的加密API，用于对传入的消息进行加密。我们将使用AES（高级加密标准）加密算法，这是一种广泛使用的对称加密算法。流程序列如图9 - 12所示。

这里我们的端点是“/encrypt/:message/key/:key”，其中“message”是要加密的实际内容，“key”是用于加密的密钥。在准备加密函数节点中，我们编写以下代码：
```javascript
// 加载CryptoJS库
var CryptoJS = require('./crypto - js.min.js');
// 准备加密
var encrypted = CryptoJS.AES.encrypt(
    msg.req.params.message,
    msg.req.params.key
).toString();
msg.payload = {
    encrypted: encrypted
};
return msg;
```
在上述代码中，我们首先加载CryptoJS库。然后，我们使用AES加密算法对传入的消息进行加密，加密密钥是从API调用中获取的。加密后的结果被转换为字符串格式，并作为消息对象的有效负载返回。

下面是使用cURL对这个API进行测试的示例：
```bash
# curl -X GET "https://www.in24hrs.xyz:1880/encrypt/HelloWorld/key/MySecretKey"
```
输出：
```json
{"encrypted":"U2FsdGVkX19n9m92877c69777769656873656e6465725365637265744b6579"}
```
从输出中可以看到，原始消息“HelloWorld”被加密成了一个看似随机的字符串。需要注意的是，在实际应用中，密钥的管理非常重要，确保密钥的安全性是保证加密有效性的关键。
#### 总结
在本章中，我们完成了REST接口的构建，涵盖了数据访问API和实用工具API。我们通过不同的条件和方式实现了数据的获取、删除等操作，还添加了诸如时间戳获取、随机代码生成、UUID生成、短信和邮件发送以及加密等实用工具功能。

在下一章中，我们将专注于平台的安全性，探讨如何确保平台免受各种潜在威胁的侵害，以及如何对平台进行监控和管理，以保证其稳定运行。

## 第10章 规则引擎与认证
规则引擎是我们物联网平台乃至任何物联网平台中最强大的模块之一。物联网平台连接环境中发生的任何事情都能触发其他事件，这一特性使得规则引擎成为关键模块。

在本章中，我们将：
- 确立规则引擎的工作逻辑
- 构建相关的规则引擎流程
- 创建用于规则管理的API
- 理解认证逻辑并加以实现
#### 从规则引擎逻辑入手
构建规则引擎基础设施的方式有很多种，并没有绝对的对错之分，只有是否巧妙、是否高效的区别。同时，规则引擎的价值与其所能提供的简洁性以及达成预期结果的能力直接相关。如果为了实现结果需要构建过多的东西，那么这种构建的价值就会降低。

基于此，我们可以通过两种方式构建规则引擎。一种是利用Node - RED基于流程的方法，简单添加一个消息流监听器，它可以根据预先编程的标准将消息分发给其他多个节点，然后执行进一步的操作。这里的关键词是“预先编程”。这意味着在部署规则之前，我们总是需要手动预先编写规则，这种方式在一定程度上比较僵化。要是我们需要动态添加新规则，或者根据其他规则的输出激活或停用现有规则该怎么办呢？这些情况促使我们采用另一种方法：基于查询的规则。这就是我们现在要构建的内容。
#### 创建数据库
构建基于查询的规则引擎的第一步是定义数据模式，并在我们的时间序列数据存储中添加一个数据表。让我们创建这个规则表，如图10 - 1所示。
| 序号 | 名称           | 类型          | 排序规则          | 属性 | 是否为空 | 默认值 | 注释     | 额外信息 |
| ---- | -------------- | ------------- | ----------------- | ---- | -------- | ------ | -------- | -------- |
| 1    | id             | int(11)       |                   |      | 否       | 无     | 自动递增 |          |
| 2    | ruleName       | varchar(255)  | latin1_swedish_ci |      | 否       | 无     |          |          |
| 3    | active         | binary(1)     |                   |      | 否       |        |          |          |
| 4    | topicPattern   | varchar(1024) | latin1_swedish_ci |      | 否       | 无     |          |          |
| 5    | payloadPattern | varchar(2048) | latin1_swedish_ci |      | 否       | 无     |          |          |
| 6    | method         | varchar(7)    | latin1_swedish_ci |      | 否       | GET    |          |          |
| 7    | webHook        | varchar(1024) | latin1_swedish_ci |      | 否       | 无     |          |          |

我们将其命名为ruleEngine表。在这个数据表中，我们有一个主键且值自动递增的id字段，这是后续引用任何规则所必需的。ruleName是规则的可读名称。active列是二进制类型，用于定义规则是否处于激活状态。有两个列定义了规则的逻辑：topicPattern和payloadPattern。这些列包含类似SQL的模式，用于规则匹配；基于匹配结果，我们可以使用webHook字段的值，并按照method列中定义的方法进行调用。默认情况下，method设置为GET请求。

我们可以基于这个结构进行多种扩展。比如，添加更多列来存储各种其他要执行的操作、要更改的值、要执行的脚本，或者受影响的全局变量等。我们也可以将这些操作放在webHook URL指向的另一个Web服务或脚本文件中，这样更为简洁。

创建完这个表后，一个典型的条目如图10 - 2所示。

让我们看看表中添加的内容。一开始，我们将这个规则命名为“timestamp rule”，这个名称显示在ruleName列中。下一个字段active设置为1，表示该规则处于激活状态。如果我们想禁用这个规则，只需将active设置为0。topicPattern字段的值为“timestamp%”，payloadPattern字段的值为“%”，这是类似于SQL查询的模式。从规则的角度来看，这意味着如果接收到的主题与这个模式匹配（即主题以“timestamp”开头，后面可以是任意内容），并且有效负载可以是任意内容，那么该规则就会执行。最后两个字段定义了webHook信息：method字段定义了调用类型，webHook字段包含实际要调用的URL。
#### 构建流程序列
有了这个示例条目后，让我们为规则的流程内执行创建一个序列。图10 - 3展示了创建的序列。

注意，我们已经有一个数据库监听器，并且将规则序列与它关联起来。这样可以使我们在消息流接收到消息时更快地执行规则。

在这里，我们将第一个函数块“创建规则查询”定义如下：
```javascript
// 创建规则查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "SELECT id,ruleName,topicPattern,payloadPattern,method,webHook" +
    " FROM ruleEngine WHERE" +
    " active=1" +
    " AND (" + msg.req.authFilter + ")" +
    " AND topicPattern LIKE '" + msg.topic.replace(/\*/g, "%") + "'" +
    " AND payloadPattern LIKE '" + msg.payload.replace(/\*/g, "%") + "';";
return msg;
```
在这个代码片段中，我们从数据库中选择所有处于激活状态的规则，并且这些规则的主题模式和有效负载模式要与接收到的消息相匹配。注意，我们使用`replace`函数将消息中的通配符`*`替换为SQL通配符`%`，这样可以确保模式匹配的正确性。

MySQL节点执行这个查询，并将结果返回给下一个函数节点“准备规则消息”。这个函数节点的代码如下：
```javascript
// 准备规则消息
if (msg.payload.length > 0) {
    for (var i = 0; i < msg.payload.length; i++) {
        var rule = msg.payload[i];
        var newMsg = {
            topic: rule.webHook,
            payload: msg.payload,
            method: rule.method
        };
        node.send(newMsg);
    }
}
return null;
```
在这个函数中，我们遍历查询结果中的每一条规则。对于每一条匹配的规则，我们创建一个新的消息对象，其中包含要调用的WebHook URL、原始消息的有效负载以及调用方法。然后，我们将这个新的消息对象发送出去，以便后续的HTTP请求节点进行处理。

最后，HTTP请求节点根据消息对象中的信息调用相应的WebHook URL。如果规则匹配成功，就会触发WebHook调用，执行预定义的操作。

#### 创建规则管理API
为了方便管理规则，我们需要创建一些API来对规则进行增删改查操作。图10 - 4展示了规则管理API的流程序列。

我们创建了以下几个API端点：
- `/rule/add`：用于添加新规则。
- `/rule/delete/:id`：用于删除指定ID的规则。
- `/rule/update/:id`：用于更新指定ID的规则。
- `/rule/get/:id`：用于获取指定ID的规则。
- `/rule/get/all`：用于获取所有规则。

下面是添加规则API的“创建添加规则查询”函数节点的代码：
```javascript
// 创建添加规则查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "INSERT INTO ruleEngine (ruleName, active, topicPattern, payloadPattern, method, webHook)" +
    " VALUES ('" + msg.req.body.ruleName + "'," +
    " '" + msg.req.body.active + "'," +
    " '" + msg.req.body.topicPattern.replace(/\*/g, "%") + "'," +
    " '" + msg.req.body.payloadPattern.replace(/\*/g, "%") + "'," +
    " '" + msg.req.body.method + "'," +
    " '" + msg.req.body.webHook + "')" +
    " AND (" + msg.req.authFilter + ");";
return msg;
```
在这个代码中，我们从HTTP请求的主体中获取规则的相关信息，并构建一个SQL插入语句将新规则添加到数据库中。同样，我们将消息中的通配符`*`替换为SQL通配符`%`。

删除规则API的“创建删除规则查询”函数节点的代码如下：
```javascript
// 创建删除规则查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "DELETE FROM ruleEngine WHERE id = " + msg.req.params.id +
    " AND (" + msg.req.authFilter + ");";
return msg;
```
这个函数根据请求中的规则ID构建一个SQL删除语句，从数据库中删除指定的规则。

更新规则API的“创建更新规则查询”函数节点的代码如下：
```javascript
// 创建更新规则查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
msg.topic = "UPDATE ruleEngine SET " +
    " ruleName = '" + msg.req.body.ruleName + "'," +
    " active = '" + msg.req.body.active + "'," +
    " topicPattern = '" + msg.req.body.topicPattern.replace(/\*/g, "%") + "'," +
    " payloadPattern = '" + msg.req.body.payloadPattern.replace(/\*/g, "%") + "'," +
    " method = '" + msg.req.body.method + "'," +
    " webHook = '" + msg.req.body.webHook + "'" +
    " WHERE id = " + msg.req.params.id +
    " AND (" + msg.req.authFilter + ");";
return msg;
```
这个函数根据请求中的规则ID和新的规则信息构建一个SQL更新语句，更新数据库中指定规则的信息。

获取规则API的“创建获取规则查询”函数节点的代码如下：
```javascript
// 创建获取规则查询
// 如果未定义或不可用身份验证过滤器
// 设置默认值为1
if (!msg.req.authFilter) {
    msg.req.authFilter = 1;
}
if (msg.req.params.id) {
    msg.topic = "SELECT * FROM ruleEngine WHERE id = " + msg.req.params.id +
        " AND (" + msg.req.authFilter + ");";
} else {
    msg.topic = "SELECT * FROM ruleEngine WHERE (" + msg.req.authFilter + ");";
}
return msg;
```
这个函数根据请求中是否包含规则ID，构建相应的SQL查询语句，从数据库中获取指定规则或所有规则的信息。

#### 理解认证逻辑并加以实现
在前面的所有API和规则引擎的实现中，我们都预留了`authFilter`字段。现在是时候实现认证逻辑了。

我们将使用JSON Web Token（JWT）来实现认证。JWT是一种开放标准（RFC 7519），它定义了一种紧凑且自包含的方式，用于在各方之间安全地传输信息。

首先，我们需要安装`jsonwebtoken`库。在Node - RED的工作目录中执行以下命令：
```bash
npm install jsonwebtoken
```
接下来，我们创建一个认证中间件，用于验证传入请求的JWT。图10 - 5展示了认证中间件的流程序列。

“验证JWT”函数节点的代码如下：
```javascript
// 验证JWT
var jwt = require('jsonwebtoken');
var secretKey = 'your_secret_key'; // 请替换为实际的密钥
try {
    var token = msg.req.headers['authorization'].split(' ')[1];
    var decoded = jwt.verify(token, secretKey);
    msg.req.authFilter = decoded.authFilter;
    return msg;
} catch (error) {
    msg.statusCode = 401;
    msg.payload = { error: 'Unauthorized' };
    return msg;
}
```
在这个函数中，我们从请求的`Authorization`头中提取JWT，并使用预定义的密钥对其进行验证。如果验证成功，我们将JWT中包含的`authFilter`信息添加到请求对象中，以便后续的查询使用。如果验证失败，我们返回一个401状态码和错误信息。

为了生成JWT，我们创建一个“生成JWT”函数节点，代码如下：
```javascript
// 生成JWT
var jwt = require('jsonwebtoken');
var secretKey = 'your_secret_key'; // 请替换为实际的密钥
var payload = {
    authFilter: msg.req.body.authFilter
};
var token = jwt.sign(payload, secretKey, { expiresIn: '1h' });
msg.payload = { token: token };
return msg;
```
这个函数根据请求主体中的`authFilter`信息生成一个JWT，并将其作为响应返回。

#### 总结
在本章中，我们构建了一个强大的规则引擎，允许根据接收到的消息动态执行规则。通过创建规则管理API，我们可以方便地对规则进行增删改查操作。同时，我们实现了基于JWT的认证逻辑，确保API和规则引擎的安全性。

在下一章中，我们将探讨如何对物联网平台进行监控和调试，以确保其稳定运行。 

## 第11章 文档编写与测试
良好的文档能够加速所开发资源的开发和使用，还能减少用于回复支持请求的时间和资金成本。文档通常被视为整体用户体验的一部分，完整且准确的文档始终是节省资源、提高API使用效率和效果的关键。

由此产生的一个明显问题是，我们如何有效地为物联网平台的API编写文档呢？最受欢迎的方法之一是使用免费或开源工具来进行API文档编写。

我们将使用Swagger工具来生成API文档。使用Swagger可以轻松创建交互式文档，并且在后续维护过程中也毫不费力。更重要的是，你既可以将接口（定义和沙盒）托管在Swagger hub上，也可以将其作为独立组件集成到单独的云实例中。我们在本书中采用的就是后者。

在本章中，我们将：
- 讨论如何准备OpenAPI规范
- 克隆并更新Swagger包
- 在实时环境中测试API文档
#### 准备有效的OpenAPI规范文档
创建有用文档的第一步是使用有效的格式创建API描述文档。由于Swagger使用OpenAPI规范，且该规范可以用YAML格式进行文档编写，所以我们首先要创建YAML格式的文档。

YAML（YAML Ain’t Markup Language）是一种人类可读的数据序列化语言，通常用于配置文件，也可用于许多数据存储（如调试输出）或传输（如文档头）的应用场景。

如果你对学习OpenAPI格式和规范感兴趣，互联网上有很多教程，Swagger网站上也有不少。

Swagger在线编辑器是另一个开源免费工具，可用于验证我们正在准备的规范文档，或者我们也可以在该编辑器中从头开始创建文档。该编辑器的网址是https://editor.swagger.io/#/ 。

在撰写本书时，我创建了一个API规范文档（api.yml），你可以从GitHub上获取。下载该文件（.yml格式，即YAML格式），将文件内容粘贴到Swagger在线编辑器中，确保没有警告或错误。在Swagger编辑器右侧的视图中，你看到的是部署后的实际用户界面。

你可以对文档进行修改，看看这些修改如何影响输出结果。完成后，将文件内容复制并粘贴回本地副本，以便进一步使用。
#### 平台API规范文件详解
现在让我们参考平台API文件的内容，详细了解文件结构。
```yaml
swagger: '2.0'
info:
  title: in24hrs.the.book
  description: This page provides documentation and also serves as a test sandbox for **in24hrs** platform.
  contact:
    email: in24hrs@anandtamboli.com
  version: "1.0.1"
schemes:
  - https
host: 'www.in24hrs.xyz:1880'
basePath: /
```
第一行声明了API规范的版本，在我们的例子中是2.0。接下来的几行提供了关于文档的一些描述，这些描述也会显示在用户界面上。我们指定了API调用将遵循的方案，这里固定为仅使用HTTPS。Host和basePath的定义指定了API的基础URL。

需要注意的是，此文件中任何以#字符开头的行都被视为注释。
```yaml
securityDefinitions:
  Bearer:
    type: apiKey
    name: authorization
    in: header
```
接下来的几行定义了API的安全/认证方案。在上一章中，我们为API添加了Bearer认证，并创建了一种方案，要求在授权头中传递认证信息。这些行定义了该方案，以便在使用这个交互式API UI时，我们可以向其提供认证详细信息。
```yaml
paths:
  #--------------------------------------------------------------
  # 1 Data publishing API
  /pub/{topic}/{payload}:
    post:
      description: 'create a new data {payload} for a {topic}'
      security:
        - Bearer: []
      tags:
        - '1 Data publishing API'
      parameters:
        - name: topic
          in: path
          type: string
          description: 'Topic to publish data for'
          required: true
        - name: payload
          in: path
          description: 'Message payload for given topic, could be plain text, binary, json, or any other format'
          type: string
          required: true
      responses:
        '200':
  description: 'Successfully published data'
  schema:
    type: object
    properties:
      id:
        type: integer
      topic:
        type: string
      payload:
        type: string
      timestamp:
        type: string
```

这部分 YAML 定义了 `/pub/{topic}/{payload}` 路径的 POST 请求。它描述了该 API 用于为指定主题创建新数据。`security` 字段表明该 API 需要使用之前定义的 `Bearer` 认证。`parameters` 部分定义了路径参数 `topic` 和 `payload`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `id`、`topic`、`payload` 和 `timestamp` 等属性。

```yaml
#--------------------------------------------------------------
# 2 Data retrieval API
/get/{topic}/last/{count}:
  get:
    description: 'Get last {count} data records for a {topic}'
    security:
      - Bearer: []
    tags:
      - '2 Data retrieval API'
    parameters:
      - name: topic
        in: path
        type: string
        description: 'Topic to retrieve data for'
        required: true
      - name: count
        in: path
        type: integer
        description: 'Number of last records to retrieve'
        required: true
    responses:
      '200':
        description: 'Successfully retrieved data'
        schema:
          type: array
          items:
            type: object
            properties:
              id:
                type: integer
              topic:
                type: string
              payload:
                type: string
              timestamp:
                type: string
```
此部分定义了 `/get/{topic}/last/{count}` 路径的 GET 请求。它描述了该 API 用于获取指定主题的最后 `count` 条数据记录。同样，该 API 需要 `Bearer` 认证。`parameters` 部分定义了路径参数 `topic` 和 `count`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，是一个包含多个对象的数组，每个对象包含 `id`、`topic`、`payload` 和 `timestamp` 等属性。

```yaml
#--------------------------------------------------------------
# 3 Data deletion API
/delete/{topic}/id/{id}:
  get:
    description: 'Mark a single record with {id} as deleted for a {topic}'
    security:
      - Bearer: []
    tags:
      - '3 Data deletion API'
    parameters:
      - name: topic
        in: path
        type: string
        description: 'Topic to delete data from'
        required: true
      - name: id
        in: path
        type: integer
        description: 'ID of the record to mark as deleted'
        required: true
    responses:
      '200':
        description: 'Successfully marked record as deleted'
        schema:
          type: object
          properties:
            found:
              type: integer
            changed:
              type: integer
```
这部分定义了 `/delete/{topic}/id/{id}` 路径的 GET 请求。它描述了该 API 用于将指定主题下具有特定 `id` 的单条记录标记为已删除。该 API 需要 `Bearer` 认证。`parameters` 部分定义了路径参数 `topic` 和 `id`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `found` 和 `changed` 两个属性。

```yaml
#--------------------------------------------------------------
# 4 Microservices API
/timestamp:
  get:
    description: 'Get current timestamp'
    tags:
      - '4 Microservices API'
    responses:
      '200':
        description: 'Successfully retrieved timestamp'
        schema:
          type: object
          properties:
            timestamp:
              type: string
```
此部分定义了 `/timestamp` 路径的 GET 请求。它描述了该 API 用于获取当前时间戳。该 API 不需要认证（没有 `security` 字段）。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `timestamp` 属性。

```yaml
/randomcode/{len}:
  get:
    description: 'Generate a random alphanumeric string of {len} characters'
    tags:
      - '4 Microservices API'
    parameters:
      - name: len
        in: path
        type: integer
        description: 'Length of the random string to generate'
        required: true
    responses:
      '200':
        description: 'Successfully generated random code'
        schema:
          type: object
          properties:
            code:
              type: string
```
这部分定义了 `/randomcode/{len}` 路径的 GET 请求。它描述了该 API 用于生成指定长度的随机字母数字字符串。`parameters` 部分定义了路径参数 `len`，并说明了它的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `code` 属性。

```yaml
/uuid:
  get:
    description: 'Generate a UUID'
    tags:
      - '4 Microservices API'
    responses:
      '200':
        description: 'Successfully generated UUID'
        schema:
          type: object
          properties:
            uuid:
              type: string
```
此部分定义了 `/uuid` 路径的 GET 请求。它描述了该 API 用于生成一个 UUID。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `uuid` 属性。

```yaml
/sms/to/{to}/message/{message}:
  get:
    description: 'Send an SMS message to {to}'
    tags:
      - '4 Microservices API'
    parameters:
      - name: to
        in: path
        type: string
        description: 'Phone number to send SMS to'
        required: true
      - name: message
        in: path
        type: string
        description: 'Message to send in the SMS'
        required: true
    responses:
      '200':
        description: 'Successfully queued SMS for sending'
        schema:
          type: object
          properties:
            smsTo:
              type: string
            message:
              type: string
            status:
              type: string
```
这部分定义了 `/sms/to/{to}/message/{message}` 路径的 GET 请求。它描述了该 API 用于向指定电话号码发送短信。`parameters` 部分定义了路径参数 `to` 和 `message`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `smsTo`、`message` 和 `status` 等属性。

```yaml
/email/to/{to}/subject/{subject}/message/{message}:
  get:
    description: 'Send an email to {to}'
    tags:
      - '4 Microservices API'
    parameters:
      - name: to
        in: path
        type: string
        description: 'Email address to send email to'
        required: true
      - name: subject
        in: path
        type: string
        description: 'Subject of the email'
        required: true
      - name: message
        in: path
        type: string
        description: 'Message body of the email'
        required: true
    responses:
      '200':
        description: 'Successfully queued email for sending'
        schema:
          type: object
          properties:
            to:
              type: string
            status:
              type: string
```
此部分定义了 `/email/to/{to}/subject/{subject}/message/{message}` 路径的 GET 请求。它描述了该 API 用于向指定电子邮件地址发送邮件。`parameters` 部分定义了路径参数 `to`、`subject` 和 `message`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `to` 和 `status` 等属性。

```yaml
/encrypt/{message}/key/{key}:
  get:
    description: 'Encrypt {message} using {key}'
    tags:
      - '4 Microservices API'
    parameters:
      - name: message
        in: path
        type: string
        description: 'Message to encrypt'
        required: true
      - name: key
        in: path
        type: string
        description: 'Encryption key'
        required: true
    responses:
      '200':
        description: 'Successfully encrypted message'
        schema:
          type: object
          properties:
            encrypted:
              type: string
```
这部分定义了 `/encrypt/{message}/key/{key}` 路径的 GET 请求。它描述了该 API 用于使用指定密钥对消息进行加密。`parameters` 部分定义了路径参数 `message` 和 `key`，并说明了它们的类型、描述以及是否为必需项。`responses` 部分定义了成功响应（状态码 200）时返回的数据结构，包含 `encrypted` 属性。

### 克隆并更新 Swagger 包
现在我们已经准备好 OpenAPI 规范文档，接下来需要克隆 Swagger 包并进行必要的更新。

首先，从 GitHub 克隆 Swagger UI 仓库：
```bash
git clone https://github.com/swagger-api/swagger-ui.git
cd swagger-ui
```
进入克隆的目录后，我们需要更新 `dist/index.html` 文件。找到如下代码行：
```html
<script>
  window.onload = function() {
    // Begin Swagger UI call region
    const ui = SwaggerUIBundle({
      url: "https://petstore.swagger.io/v2/swagger.json",
      dom_id: '#swagger-ui',
      deepLinking: true,
      presets: [
        SwaggerUIBundle.presets.apis,
        SwaggerUIStandalonePreset
      ],
      plugins: [
        SwaggerUIBundle.plugins.DownloadUrl
      ],
      layout: "StandaloneLayout"
    })
    // End Swagger UI call region

    window.ui = ui
  }
</script>
```
将 `url` 参数的值从 `"https://petstore.swagger.io/v2/swagger.json"` 替换为我们本地的 `api.yml` 文件的路径。假设我们将 `api.yml` 文件放在 `swagger-ui` 目录下，那么更新后的代码如下：
```html
<script>
  window.onload = function() {
    // Begin Swagger UI call region
    const ui = SwaggerUIBundle({
      url: "api.yml",
      dom_id: '#swagger-ui',
      deepLinking: true,
      presets: [
        SwaggerUIBundle.presets.apis,
        SwaggerUIStandalonePreset
      ],
      plugins: [
        SwaggerUIBundle.plugins.DownloadUrl
      ],
      layout: "StandaloneLayout"
    })
    // End Swagger UI call region

    window.ui = ui
  }
</script>
```

### 在实时环境中测试 API 文档
更新完 `dist/index.html` 文件后，我们可以在本地启动一个简单的 HTTP 服务器来测试 Swagger UI。在 `swagger-ui` 目录下执行以下命令：
```bash
python3 -m http.server 8080
```
这将在本地的 8080 端口启动一个 HTTP 服务器。打开浏览器，访问 `http://localhost:8080/dist`，你应该能看到 Swagger UI 界面，其中展示了我们的 API 文档。

你可以在这个界面上测试各个 API 端点。点击每个端点旁边的 “Try it out” 按钮，输入必要的参数，然后点击 “Execute” 按钮，即可发送请求并查看响应结果。

### 总结
在本章中，我们学习了如何使用 Swagger 工具为物联网平台的 API 编写文档。我们首先准备了 OpenAPI 规范的 YAML 文件，详细介绍了文件的结构和内容。然后，我们克隆了 Swagger UI 仓库，并更新了相关配置以使用我们自己的 API 规范文档。最后，我们在本地启动了一个 HTTP 服务器来测试 API 文档，通过 Swagger UI 界面可以方便地测试各个 API 端点。

在下一章中，我们将探讨如何对物联网平台进行部署和扩展，以满足不同的使用场景和需求。 

## 第12章 部署与扩展
到目前为止，我们已经成功构建了一个物联网平台，并且为其 API 编写了文档。现在是时候将这个平台部署到生产环境中，并考虑如何对其进行扩展以满足不断增长的需求了。

在本章中，我们将：
- 探讨不同的部署选项
- 介绍如何使用 Docker 进行容器化部署
- 讨论平台的扩展策略
#### 部署选项
在部署物联网平台时，我们有多种选择，每种选择都有其优缺点。以下是一些常见的部署选项：
- **裸机服务器**：在裸机服务器上直接部署平台意味着你拥有对硬件和软件的完全控制权。你可以根据自己的需求进行定制化配置，安装所需的操作系统和软件包。然而，这种方式需要更多的技术知识和管理工作，包括服务器的维护、硬件的升级等。
- **虚拟专用服务器（VPS）**：VPS 是一种虚拟化的服务器，它提供了独立的计算资源，如 CPU、内存和存储。与裸机服务器相比，VPS 的管理相对简单，你可以通过控制面板轻松地进行配置和管理。同时，VPS 的成本通常比裸机服务器低。
- **云服务提供商**：使用云服务提供商（如 Amazon Web Services、Google Cloud Platform 或 Microsoft Azure）可以让你轻松地部署和扩展平台。这些云服务提供商提供了各种服务，如计算、存储、数据库等，你可以根据自己的需求选择合适的服务。此外，云服务提供商还提供了高可用性和容错能力，确保平台的稳定运行。
- **容器化部署**：容器化部署是一种轻量级的虚拟化技术，它将应用程序及其依赖项打包成一个独立的容器。容器可以在不同的环境中运行，确保应用程序的一致性和可移植性。Docker 是最流行的容器化平台之一，我们将在后面的章节中详细介绍如何使用 Docker 进行部署。

#### 使用 Docker 进行容器化部署
Docker 是一个开源的容器化平台，它可以帮助我们将应用程序及其依赖项打包成一个独立的容器。使用 Docker 进行部署有以下优点：
- **一致性**：容器包含了应用程序运行所需的所有依赖项，确保在不同的环境中都能一致运行。
- **可移植性**：容器可以在任何支持 Docker 的环境中运行，包括开发、测试和生产环境。
- **快速部署**：容器的启动和停止速度非常快，可以实现快速的部署和更新。
- **资源隔离**：容器之间相互隔离，不会相互影响，提高了系统的安全性和稳定性。

##### 步骤 1：安装 Docker
首先，你需要在服务器上安装 Docker。不同的操作系统有不同的安装方法，你可以参考 Docker 官方文档进行安装。以下是在 Ubuntu 系统上安装 Docker 的示例命令：
```bash
# 更新系统包列表
sudo apt-get update

# 安装必要的依赖包
sudo apt-get install apt-transport-https ca-certificates curl software-properties-common

# 添加 Docker 的官方 GPG 密钥
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -

# 添加 Docker 的软件源
sudo add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"

# 更新系统包列表
sudo apt-get update

# 安装 Docker
sudo apt-get install docker-ce
```

##### 步骤 2：创建 Dockerfile
Dockerfile 是一个文本文件，用于定义 Docker 镜像的构建过程。以下是一个简单的 Dockerfile 示例，用于构建我们的物联网平台镜像：
```Dockerfile
# 使用 Node.js 作为基础镜像
FROM node:14

# 设置工作目录
WORKDIR /app

# 复制 package.json 和 package-lock.json 到工作目录
COPY package*.json ./

# 安装依赖项
RUN npm install

# 复制应用程序代码到工作目录
COPY . .

# 暴露应用程序的端口
EXPOSE 1880

# 启动应用程序
CMD ["npm", "start"]
```

##### 步骤 3：构建 Docker 镜像
在 Dockerfile 所在的目录下，执行以下命令来构建 Docker 镜像：
```bash
docker build -t iot-platform .
```
其中，`-t` 参数用于指定镜像的标签，`iot-platform` 是我们给镜像起的名称，`.` 表示使用当前目录下的 Dockerfile 进行构建。

##### 步骤 4：运行 Docker 容器
构建完成后，我们可以使用以下命令来运行 Docker 容器：
```bash
docker run -d -p 1880:1880 iot-platform
```
其中，`-d` 参数表示在后台运行容器，`-p` 参数用于将容器的端口映射到主机的端口，这里将容器的 1880 端口映射到主机的 1880 端口。

#### 平台的扩展策略
随着物联网设备数量的增加和数据流量的增长，我们需要考虑如何对平台进行扩展以满足不断增长的需求。以下是一些常见的扩展策略：
- **水平扩展**：水平扩展是指通过增加服务器的数量来提高平台的处理能力。你可以使用负载均衡器将请求分发到多个服务器上，从而实现水平扩展。例如，使用 Nginx 或 HAProxy 作为负载均衡器。
- **垂直扩展**：垂直扩展是指通过增加单个服务器的硬件资源（如 CPU、内存和存储）来提高平台的处理能力。这种方式相对简单，但成本较高，并且有一定的限制。
- **数据库扩展**：数据库是物联网平台的核心组件之一，随着数据量的增加，数据库的性能可能会成为瓶颈。你可以考虑使用分布式数据库（如 Cassandra 或 MongoDB）来实现数据库的扩展。
- **缓存机制**：使用缓存机制可以减少对数据库的访问，提高平台的响应速度。你可以使用 Redis 或 Memcached 作为缓存服务器。

### 总结
在本章中，我们探讨了不同的部署选项，包括裸机服务器、VPS、云服务提供商和容器化部署。我们详细介绍了如何使用 Docker 进行容器化部署，包括安装 Docker、创建 Dockerfile、构建 Docker 镜像和运行 Docker 容器。此外，我们还讨论了平台的扩展策略，包括水平扩展、垂直扩展、数据库扩展和缓存机制。

通过合理选择部署选项和扩展策略，我们可以确保物联网平台在生产环境中稳定运行，并能够满足不断增长的需求。在下一章中，我们将对本书进行总结，并展望物联网平台的未来发展趋势。 

## 术语表
- **API（Application Programming Interface）**：应用程序编程接口。一组用于构建软件应用程序的规则和协议，允许不同的软件组件或系统之间进行交互和通信。
- **AES（Advanced Encryption Standard）**：高级加密标准。一种广泛使用的对称加密算法，用于保护数据的机密性。
- **Bearer Authentication**：令牌认证。一种使用令牌（通常是 JWT）来验证用户身份和授权访问资源的认证方式。
- **Cloud - Agnostic**：云无关性。指系统或应用程序不依赖于特定的云服务提供商，可以在不同的云环境中运行。
- **CryptoJS**：一个 JavaScript 库，用于实现各种加密算法，如 AES、SHA - 256 等。
- **Docker**：一个开源的容器化平台，用于将应用程序及其依赖项打包成独立的容器，实现应用程序的快速部署和可移植性。
- **JWT（JSON Web Token）**：JSON Web 令牌。一种开放标准（RFC 7519），用于在各方之间安全地传输声明。它通常由三部分组成：头部、载荷和签名。
- **LoRaWAN**：一种低功耗广域网（LPWAN）协议，用于物联网设备的长距离通信。
- **Middleware**：中间件。位于操作系统和应用程序之间的软件层，提供各种服务和功能，如数据传输、协议转换、安全认证等。
- **Node - RED**：一个基于流的编程工具，用于连接硬件设备、API 和在线服务。它使用可视化的节点和流来创建应用程序逻辑。
- **OpenAPI Specification**：OpenAPI 规范。一种用于描述 RESTful API 的标准格式，通常使用 YAML 或 JSON 编写。它可以用于生成 API 文档、测试工具和客户端代码。
- **REST（Representational State Transfer）**：表述性状态转移。一种软件架构风格，用于设计网络应用程序的 API。RESTful API 使用 HTTP 协议进行通信，通过不同的 HTTP 方法（如 GET、POST、PUT、DELETE）来操作资源。
- **Swagger**：一个用于设计、构建、记录和使用 RESTful API 的开源工具集。它可以根据 OpenAPI 规范生成交互式的 API 文档，并提供测试和调试功能。
- **Time - Series Database**：时间序列数据库。一种专门用于存储和管理时间序列数据的数据库，适用于物联网、监控和日志分析等场景。
- **UUID（Universally Unique Identifier）**：通用唯一识别码。一种由数字和字母组成的 128 位标识符，用于在分布式系统中唯一地标识资源。
- **WebHook**：一种 Web 回调机制，当特定事件发生时，服务器会自动向预先注册的 URL 发送 HTTP 请求，通知其他系统或应用程序。
- **YAML（YAML Ain’t Markup Language）**：一种人类可读的数据序列化语言，常用于配置文件和数据交换。它的语法简洁，易于阅读和编写。 

## 索引
- **A**
    - AES（高级加密标准），109
    - API（应用程序编程接口）
        - 认证，119
        - 文档编写，127
        - 管理，115
        - 微服务，107
        - 规则引擎，112
        - RESTful，93
        - 测试，135
- **B**
    - Bearer认证，119
- **C**
    - 缓存机制，145
    - 容器化部署，141
    - CryptoJS，109
- **D**
    - 数据访问，93
    - 数据库扩展，145
    - 数据库，112
    - 调试，101
- **E**
    - 加密实用工具，109
- **F**
    - 免费增值平台，23
- **G**
    - 网关，87
- **H**
    - 硬件无关性，20
- **I**
    - 物联网（IoT）
        - 背景，13
        - 平台类型，16
        - 解决方案，83
- **J**
    - JWT（JSON Web Token），119
- **L**
    - LoRaWAN，16
- **M**
    - 中间件，14
    - 微服务，107
    - MySQL，112
- **N**
    - Node - RED
        - 加密，109
        - 规则引擎，112
        - 部署，89
        - 介绍，87
- **O**
    - OpenAPI规范，127
- **P**
    - 平台扩展，143
    - 平台类型，16
- **R**
    - REST接口，93
    - Redis，145
    - 规则引擎，112
- **S**
    - 安全，22
    - 短信发送，107
    - 对称加密，109
    - Swagger，127
    - 系统监控，101
- **T**
    - 时间序列数据库，112
    - 测试，135
    - 物联网平台的优点，23
- **U**
    - UUID（通用唯一识别码），107
- **V**
    - 垂直扩展，143
- **W**
    - WebHook，114
    - 网络服务平台，16
- **Y**
    - YAML，127 